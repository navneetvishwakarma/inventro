| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-25 | `uploadGroupedReceiptFiles()`: uploads 2-3 files to their own Storage objects in order, inserts ONE `receipts` row with an ordered `storage_paths` array; any failure after one or more uploads succeed removes ALL of them (extends `uploadReceiptFile`'s single-object no-orphans invariant to the plural case). `hashCombinedFileBytes()`: one SHA-256 over the concatenated ordered bytes -- deliberately catches only an identical re-submission of the same grouping+order, not the same screenshots re-uploaded individually (documented limit, not a bug). `uploadGroupedReceiptAction()`: re-validates 2-3 files, all image extensions, server-side (never trusts the client toggle alone) before calling the above. `routeExtraction()`/`runExtraction()` in `lib/llm/extract.ts` refactored to take a `pages: {bytes, mime}[]` list (downloads every `storage_paths` entry, not just `[0]`) -- a plain single-file receipt has exactly one entry, so this is a strict superset with zero behavior change there; the image branch builds one `image` part per page in order when `pages.length > 1`, with an anti-double-count instruction appended ONLY in that case. `isImageExtension()` added to `formats.ts` for the toggle's client+server validation. **Advisor-caught fix (post-initial-verify, see Epic gate row):** per-page `mediaType` is now derived from each storage path's OWN file extension (`mimeForStoragePath`), not the single shared `receipts.mime` column -- the original version stamped every page with the group's first file's mime, silently mislabeling any later page with a different image format (e.g. a PNG+JPEG pair). | lib/receipts/storage.ts, lib/receipts/dedup.ts, lib/receipts/formats.ts (isImageExtension), app/add/actions.ts (uploadGroupedReceiptAction), lib/llm/extract.ts, app/add/capture.tsx (toggle UI, committed in S-28), app/review/[id]/page.tsx + review-detail.tsx (docUrls multi-image preview, committed in S-27) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Live-Supabase + Storage + real Gemini** (no fixture receipt images exist in this repo -- confirmed via grep before building synthetic ones with `sharp`, an already-installed transitive dependency, rendering two SVG "receipt" pages to JPEG): page 1 listed Milk/Bread/Eggs/Rice/Sugar, page 2 listed Rice/Sugar (deliberate 2-item scroll overlap)/Coffee/Tea -- 7 unique items total. Grouped upload produced exactly one `receipts` row (`storage_paths` length 2, `mime='image/jpeg'`), exactly one `ingest_jobs` row (`state='done'`), `parse_path='multimodal'`, and extraction returned exactly 7 `receipt_lines` (Milk, Bread, Eggs, Rice, Sugar, Coffee, Tea) -- not 9, confirming the anti-double-count instruction worked against a real overlapping-pages scenario, not just structurally. **Post-advisor-fix re-verify:** grouped a PNG page + a JPEG page (deliberately mismatched formats) with a 2-item overlap; extraction completed cleanly (no Gemini rejection/garbling from a wrong mediaType on either page) and returned exactly 3 unique lines (Butter, Yogurt, Cheese), not 4 or an error. All rows and Storage objects created for both checks were deleted afterward. | 895e38e, 84faba9 | done | The LLM-dependent half of A24 (no triple-counting) was verified with two synthetic overlap scenarios, not a corpus of real q-commerce screenshots -- real-world OCR/layout variance could behave differently; `checkExtractionQuality`'s existing >5% order-total-deviation check remains a partial structural guard (didn't need to fire in either run, order_total was null). |
| S-26 | `lib/receipts/paste-staging.ts`: module-singleton pub/sub (`stageFile`/`subscribeStagedFile`) bridging a paste event on any page to `app/add/capture.tsx`, whether it's already mounted or mounts right after navigation. `app/paste-listener.tsx` (`'use client'`, mounted as a child of the still-server-component `app/layout.tsx`): guards on `event.target` (never intercepts a focused input/textarea/contenteditable) and on `clipboardData.items` actually containing an image (an ordinary text paste anywhere is left alone, never even calls `preventDefault()` unless an image was found); synthesizes a filename from the clipboard item's MIME type via `formats.ts`'s new `extensionForMimeType()` since a pasted blob has none (the concrete failure mode -- `isAcceptedExtension(null)` silently rejecting it -- that advisor review flagged before implementation). `app/add/capture.tsx`'s `subscribeStagedFile()` wiring (feeds a staged file into the exact same `handleFiles()` used by drag-drop/file-picker) landed in S-28's commit alongside that story's own capture.tsx changes. | lib/receipts/paste-staging.ts, app/paste-listener.tsx, app/layout.tsx, lib/receipts/formats.ts (extensionForMimeType), app/add/capture.tsx (subscribe wiring, committed in S-28) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. No headless-browser tooling is available in this environment (no playwright/puppeteer installed) so the actual `ClipboardEvent`/paste interaction could not be driven end-to-end by script -- verified instead by: (1) a real `next dev` + signed-gate-cookie curl render of `/add` confirming the page (with `PasteListener` mounted in the root layout) renders 200 with no SSR error; (2) careful code-trace of the guard order (target-check before clipboardData inspection, image-type check before `stageFile`/`preventDefault`) against the exact failure modes advisor review raised. This is a documented gap, not a silent one. | 8f62d2e | done | Client-only browser interaction (real paste event, real clipboard image) was not exercised by an automated script in this environment -- verified by code-trace and a real page-render smoke check only, not a live end-to-end paste. Flagged explicitly rather than claimed as fully proven. |
| S-27 | `app/review/[id]/page.tsx` parses an optional `session` search param (comma-separated receipt ids from an Add batch upload) into `{position, total, nextId, allIds}`, passed to `ReviewDetail`. `review-detail.tsx` renders a "Reviewing X of Y" counter (with a "Skip to next" link) when `session` is present, in both the normal editable view AND the `status==='committed'` early-return branch (advisor-caught gap: a mid-session receipt already committed out of band, e.g. a stale back-button visit, would otherwise strand the user on a bare card with no way onward). After a successful `commitReceiptAction`, client-side `router.push()` advances to the next id in session, or back to `/review` queue when done. `session` is read-only UI navigation state -- `commitReceiptAction` still validates entirely server-side against the DB row, unchanged. **Advisor-caught fix:** the FULL session id list (`allIds`) is now forwarded unchanged on every `Skip to next` link and post-commit redirect, with `indexOf(currentId)` deriving position fresh each page load. The original version forwarded only `ids.slice(index+1)` ("remaining" ids), which made the list shrink on every hop and re-derive position/total against the shrunken list -- "1 of 3" became "1 of 2" then "1 of 1" instead of "1 of 3" -> "2 of 3" -> "3 of 3"; the first verify pass hand-typed the full URL at each step instead of following the app's own generated link, so it passed vacuously. Backdating (A18) required NO new backend logic: traced through `v_current_stock` (S-02, `occurred_at >= stock_epoch`), `commit_receipt()` (S-13, writes the purchase movement at `purchased_at` exactly), and `lib/predictions/recompute.ts`'s `stock_movements` query feeding `computeItemStats` (confirmed no `stock_epoch` filter exists there, in either `recomputeOneItem` or `recomputeAllItemsForHousehold`) -- this story's contribution is verification and sequential-review UI, not new logic. | app/review/[id]/page.tsx, app/review/[id]/review-detail.tsx (also carries S-25's docUrls multi-image preview and S-28's previewText `<pre>` rendering, authored together) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Live-Supabase backdating check** (real household, a genuinely fresh catalog item confirmed to have zero pre-existing `item_stats` row -- this build has never had a real receipt committed to the real household before, checked directly rather than assumed): established a present-day purchase first (via the real `commitReceipt()` from `lib/review/commit.ts`, not a hand-rolled substitute) so the item had real "existing later history" to test against, then committed a second purchase dated 60 days before `stock_epoch`. Confirmed by direct query, all four assertions true: `v_current_stock` unchanged (200 -> 200) without the override checkbox; `item_stats.purchase_count` +1 (1 -> 2); `item_stats.last_purchased_at` UNCHANGED at the real later date (not regressed to the backdated one -- the `greatest(...)` invariant in `commit_receipt()`, genuinely exercised, not trivially true on a brand-new item); `item_stats.rate_correction` UNCHANGED (S-15 reconciliation correctly skipped for a backdated commit). A third commit WITH the override checkbox increased `v_current_stock` by exactly the override amount (200 -> 250), as `commit_receipt()`'s existing override branch predicts. All movements, `price_history`, `item_stats`, and `item_stats_history` rows created for this check were deleted, restoring the item to its true pre-check state (zero `stock_movements`, no `item_stats` row) -- confirmed by direct re-query after cleanup, not assumed. **Sequential counter, post-advisor-fix re-verify (`next dev` + signed gate cookie + curl, three throwaway `parsed` receipts, real household):** page 1 rendered "1 of 3" with a `Skip to next` href generated by the app itself; that exact href (extracted programmatically from the response, never hand-typed) was followed to page 2, which rendered "2 of 3" with its own generated href; that was followed to page 3, which rendered "3 of 3" with NO further link. This is the check that discriminates -- following the app's own links, not reconstructing URLs. All three throwaway receipts deleted afterward. | 1deab6b, 84faba9 | done | None outstanding -- both this story's core claims (A18 already holds structurally, and the counter genuinely advances 1-of-N through N-of-N) were verified against real data/real rendered links, not asserted from code-reading alone. |
| S-28 | `formats.ts`: `'txt'` added to `ACCEPTED_EXTENSIONS`/`CONTENT_TYPES` (`text/plain`) -- confirmed first (read `supabase/migrations/20260729020232_receipts_storage_bucket.sql` and grepped `full_schema.sql`) that the `receipts` Storage bucket has no `allowed_mime_types` restriction and `receipts.mime` has no DB check constraint, so this needed no migration. `app/add/paste-text.tsx`: a "Paste order text" disclosure revealing a plain `<textarea>` (styled inline to match `input.tsx`'s classes -- no new shadcn primitive for one textarea); submit wraps the text as a `.txt` `File` and calls the exact same `handleFiles()` pipeline every other format uses. `routeExtraction()`'s new `mime==='text/plain'` branch (landed in S-25's commit alongside the `byteList` refactor it depends on) decodes UTF-8 directly and returns `parsePath:'text'`. `lib/review/data.ts`'s new `getDocumentPreviewText()` downloads+decodes a text/plain object server-side for the review pane's `<pre>` rendering (S-27's commit). | lib/receipts/formats.ts (txt + CONTENT_TYPES), app/add/paste-text.tsx, lib/review/data.ts (getDocumentPreviewText), app/add/capture.tsx (mounts AddPasteText; also carries S-25's toggle, S-26's subscribe wiring, and S-27's review-link in this same commit -- all four stories' capture.tsx changes were authored together as one integrated Add-page update) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Live-Supabase + Storage + real Gemini**: pasted a realistic order-confirmation body (3 items + a delivery-fee line) through the real `uploadReceiptFile`/`runExtraction` pipeline -- resulting receipt: `mime='text/plain'`, `parse_path='text'` (never multimodal), 4 extracted lines (Paneer/Tomato/Onion as real items, "Delivery fee" correctly flagged `is_non_inventory=true`). Pasting the identical text again was correctly caught as a duplicate via the existing `findDuplicateReceipt` check -- zero new dedup logic needed. `getDocumentPreviewText()` returned the original pasted text byte-for-byte. All rows/Storage objects created for the check were deleted afterward. | a605fb9 | done | None outstanding. |
| Epic gate | Final `advisor` pass across the full changeset (all four stories), run after all four story commits landed and `npm run build` was clean. Caught two real defects and one minor one, all fixed in this same pass before shipping: **(1) BLOCKING** the S-27 session counter reset on every advance (see S-27 row -- `remainingIds` slicing vs. `allIds` forwarding). **(2)** S-25's grouped upload stamped every page with the group's first file's mime, mislabeling a mixed-format (e.g. PNG+JPEG) group's later pages (see S-25 row -- `mimeForStoragePath` per-page fix). **(3) minor** `app/add/capture.tsx`'s paste-staging `useEffect` had a `[]` dep array, freezing its `handleFiles` closure's `isGrouped` value at mount -- pasting with the grouping toggle ON would have silently uploaded ungrouped instead of showing the validation error; fixed by adding `isGrouped` to the effect's deps. All three fixes re-verified live (S-25's fix via a real mixed-PNG/JPEG grouped Gemini call; S-27's fix via following the app's own generated links across a 3-receipt session, not hand-constructing URLs) before this row was written. | lib/llm/extract.ts (mimeForStoragePath), app/review/[id]/page.tsx + review-detail.tsx (allIds), app/add/capture.tsx (isGrouped dep) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, all four routes still register. | 84faba9 | done | None outstanding. |
