| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-31 (part 1: cross-cutting price_history fix) | Closes the real data gap E-8/S-21 deliberately parked: `price_history.unit_price`'s basis was never stored. Additive fix (NOT a redefinition of `unit_price`, since `lib/inventory/data.ts` already reads it for E-7's shipped sparkline/avg-price display): two new nullable columns (`price_per_base_unit`, `qty_base`) plus a `price_basis` flag (`'per_base_unit'` \| `'legacy_unverified'`, default legacy). `commit_receipt()` (S-13) derives `price_per_base_unit` primarily from `line_total/qty_base` (already cross-validated against the receipt total by the extraction escalation ladder), falling back to `unit_price * leading-numeric(qty_display) / qty_base` only when `line_total` is null AND `pack_size` is null (the multi-pack branch of `normalizeUnitToBase` ignores its `qty` argument, discovered while writing this spec -- unsafe to invert, so the fallback is skipped for those lines rather than risk a wrong number flagged trustworthy). `log_shopping_list_purchase()` (S-24) and `log_manual_purchase()` (S-29) reinterpret their existing single "Price" input as **total paid** for the logged quantity, not a per-unit rate -- neither flow ever showed a unit concept, only a bare price field next to a known quantity. UI copy changed ("Total paid") in both flows so a human filling the field isn't misled by the backend reinterpretation. Backfill for pre-existing rows is uniqueness-gated (exactly one matching `receipt_lines` row, via household_id+catalog_item_id+observed_at+unit_price) -- verified live as a genuine no-op, since `price_history` had 0 rows in both households before this migration (1157 `stock_movements` existed with zero priced rows -- no write path had ever actually recorded a real price in practice). `scripts/seed-history/` extended to generate real price data for verification (independent rng stream, confirmed not to touch S-18's existing interval-jitter sequence). | `supabase/migrations/20260730170000_price_history_base_unit_basis.sql`, `20260730171000_commit_receipt_price_per_base_unit.sql`, `20260730172000_log_shopping_list_purchase_price_per_base_unit.sql`, `20260730173000_log_manual_purchase_price_per_base_unit.sql`, `20260730174000_commit_receipt_qty_display_regex_fix.sql`, `scripts/seed-history.ts`, `scripts/seed-history/plan.ts`, `app/add/manual/manual-entry-form.tsx`, `app/shopping-list/shopping-list-item-row.tsx` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/ scripts/` -> pass. **Live Supabase, all three write paths, real fixtures cleaned up afterward, row counts confirmed back at baseline (308/237/-/-/68/-/2/2/0/0 across catalog_items/item_aliases/stock_movements/price_history/item_stats/item_stats_history/receipts/receipt_lines/shopping_lists/shopping_list_items):** `log_manual_purchase` (total paid ₹125.50 for 500 base units) -> `price_per_base_unit * qty_base = 125.50` exact, `price_basis='per_base_unit'`, raw `unit_price` unchanged. `log_shopping_list_purchase` (total paid ₹84 for 200 base units) -> same invariant, exact. `commit_receipt` line A (`line_total=200`, `qty_base=1000`) -> primary derivation, `price_per_base_unit=0.2`, invariant exact. Line B (`line_total=null`, `pack_size=null`, `qty_display='2'`, `unit_display='kg'`, `unit_price=100`, `qty_base=2000`) -> secondary fallback correctly derived `0.1`/base-unit (`100*2/2000`), invariant exact -- this caught a REAL bug in the first migration attempt: Postgres `substring(text from pattern)` returns the FIRST PARENTHESIZED SUBEXPRESSION's match, not the whole match, when the pattern has one; the original pattern `'^[0-9]+(\.[0-9]+)?'` had its only group wrapping the *optional decimal* part, so for a plain integer like `'2'` the group didn't participate and `substring` returned NULL, silently disabling the fallback for the common whole-number case -- fixed with a follow-up migration (`20260730174000`) wrapping the ENTIRE leading number in the outer group instead. Line C (`line_total=null`, `pack_size='2 x 500g'`) -> correctly left `price_basis='legacy_unverified'`, `price_per_base_unit=null`, fallback correctly skipped, raw `unit_price=50` unchanged. Backfill re-confirmed as a no-op (0 rows, matching the 0-row pre-migration baseline) after the regex fix was applied. | acac872 | done | Pre-existing (not introduced by this epic, confirmed present in the `catalog_items` count before any S-31 work began): one leftover `ZZTEST3 Survivor` fixture row from an earlier epic's (E-11) live-verification cleanup, out of scope for S-31 to fix. |
| S-31 (part 2: Insights screen) | F13/REQ-19: spend vs. household budget by category (top-level rollup, left-joined from the full category list so a zero-spend category still appears -- UX doc's explicit edge case; `households.monthly_budget` is nullable with no write path anywhere in the app yet, S-33/E-14 -- rendered as "no budget set yet," not a fabricated number or crash), forward projection of next month's committed recurring spend, top-10 spend items (trailing 90 days), price-change alerts (>15% vs. a trailing average gated at >=3 PRIOR observations, UX doc's "not enough data yet" edge case), waste report (`abs(qty_base)` since waste is stored negative per the core invariant; valued at each item's latest known price, unpriced waste explicitly flagged "value unknown" rather than valued at 0 or omitted). All aggregation (spend sums, top-N, price-alert window functions, waste totals) done server-side in 5 new SQL functions -- never fetch-all-then-reduce-in-JS over `price_history`/`stock_movements`, per this repo's own established PostgREST-1000-row-cap bug class (S-18, E-11's catalog manager). `lib/insights/data.ts` (`getBudgetSummary`, `getForwardProjection`, `getTopSpendItems`, `getPriceAlerts`, `getWasteReport`), `app/insights/page.tsx`, linked from `/` and `/inventory` (this repo's established cross-linking convention, no global nav yet). `lib/date.ts` gained `currentKolkataMonthRange()` for the calendar-month window (Kolkata timezone, same class of correctness `toKolkataDateString` already established elsewhere in the app). | `supabase/migrations/20260730180000_insights_rpcs.sql`, `lib/insights/data.ts`, `app/insights/page.tsx`, `lib/date.ts`, `app/page.tsx`, `app/inventory/page.tsx` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, `/insights` registered as a dynamic route. **Live Supabase against the reseeded demo household** (seeder extended with real price data: deterministic per-item base price + tight lognormal jitter on an INDEPENDENT rng stream from S-18's interval-jitter sequence, 4 fixture items given a deliberate +35% price jump on their latest observation): `getBudgetSummary` returned real per-category totals for the current month with zero-spend categories genuinely present at ₹0 (Bakery & Breakfast, Fresh, Pet Supplies, Stationery & Misc all confirmed ₹0, not absent); `monthlyBudget: null` correctly rendered "no budget set yet" (confirmed live: both real households have `monthly_budget IS NULL` today). `getTopSpendItems` cross-checked against the SQL's own `group by/order by/limit` (not re-deriving its own answer -- the function IS the aggregation). `getPriceAlerts` flagged exactly 3 of the 4 deliberately-jumped items -- the 4th (a monthly-cadence item) was correctly EXCLUDED by the n>=3-within-90-days gate: live-confirmed it has only 2 prior observations inside the 90-day window (3 total events in-window including the latest), a genuine and welcome confirmation the gate isn't vacuous rather than a bug -- zero false positives among the other ~66 items. `getWasteReport` verified with a throwaway fixture against the real household (one priced waste movement, one unpriced): priced item valued at exactly `abs(-200)*0.5=100`, unpriced item correctly showed `valuedAmount: null` ("value unknown"), both cleaned up afterward, row counts restored. **Page-render smoke check** (`next dev` + signed gate cookie + curl, no headless-browser tooling in this environment): `/insights` -> 200 against BOTH households -- the demo household rendered real numbers across all five sections (spot-checked ₹ figures and `SYN NNN` item names present in the response body), the real cold-start-ish household rendered a clean, non-crashing all-zero/empty state across every section ("no budget set yet," "Not enough purchase history yet," "No priced purchases," "No notable price changes," "No waste logged this month"). **Advisor epic-gate pass caught a real defect before shipping**, fixed in a follow-up commit (4716fc0): `getForwardProjection` originally used a single `suggestedQtyBase/intervalEstDays` formula for every item, which inherits S-21's pack-rounding (`suggestedQtyBase = defaultPackSize * ceil(cycleConsumption/defaultPackSize)`) -- correct for "what to buy this cycle," wrong for "what this costs per month": any item whose real per-cycle consumption falls below one pack was overstated, unboundedly (a discriminating pure-arithmetic check: `dailyRateBase=30, intervalEstDays=10, defaultPackSize=1000` gave 3.33x too high). The demo seeder's own fixture data couldn't have caught this bug (it emits `qtyBase = defaultPackSize` for every event, so both formulas agreed by construction on demo data) -- fixed to use `dailyRateBase` directly when known (unbiased), falling back to `suggestedQtyBase/intervalEstDays` only when there's no rate signal yet, matching the original spec's two-branch formula. Re-verified live post-fix: total projection dropped from ₹336,178.72 to ₹268,962.73 for the demo household, with two independently hand-derived spot checks (one `dailyRateBase`-driven item, one no-rate-signal item) matching the function's output exactly. | 2fb0345, 4716fc0 | done | (1) The seeder writes `price_history.unit_price = pricePerBaseUnit` for synthetic data, so E-7's Inventory card will render a small "avg ₹0.1x"-style figure for demo items rather than a receipt-display-unit-style number -- cosmetic, demo-data-only, not a real-household-affecting bug (real write paths never touch `unit_price`'s existing meaning). (2) `/insights` calls `getInventoryItems()` twice per page load (once inside `getForwardProjection`'s `getPlanItems()`, once directly in `getWasteReport()`) -- the same "extra query cost beats a second drifting definition" tradeoff this repo has already explicitly accepted elsewhere (S-19/S-20), not a new pattern. (3) Price-change alerts are gated to a 90-day trailing window per the UX doc's own literal wording ("trailing-90-day price average") -- a genuinely slow-cadence item (quarterly/half-yearly/yearly) may never accumulate 3 qualifying observations within any 90-day window and can never be flagged even with a real, sustained price change; a correct fix would need a longer or cadence-aware lookback, out of scope for this epic, worth a follow-up if it proves to matter in practice. (4) The pre-existing `normalizeUnitToBase` multi-pack-quantity bug found while designing the `commit_receipt` price fallback (the "N x Munit" branch ignores its own `qty` argument, so buying >1 of a multi-pack undercounts `qty_base`) is real but out of scope for this epic -- not fixed, noted for a future story. |
| Epic gate | Two `advisor` passes across the changeset. **First pass** (after both S-31 commits landed, build clean): confirmed the additive-columns-not-redefinition design was correct, then caught the `getForwardProjection` pack-rounding bias described above -- blocking, fixed in commit 4716fc0 and re-verified live before shipping. **Second implicit pass**: nothing else flagged as blocking; the three residual risks above (seeder cosmetic price display, double `getInventoryItems()` fetch, 90-day price-alert window) were explicitly named as acceptable, not silently left. | -- | `npm run build` clean after the fix; all three original write paths (commit_receipt, log_shopping_list_purchase, log_manual_purchase) re-verified live post-fix with no regressions (row counts restored to baseline throughout). | -- | done | See story-level risk list above; nothing additional at the epic level. |
