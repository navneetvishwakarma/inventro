| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-33 | REQ-21: /settings screen -- household config form (name, monthly_budget, notify_email), a pointer link to the pre-existing /catalog manager (no new catalog UI built), JSON export (full-fidelity multi-table snapshot: household incl. its own notify_email since this is a self-directed download, catalog_items+category_name, stock_movements, price_history, receipts metadata -- storage_paths excluded as internal Storage keys), CSV export (one flat purchase/movement ledger row per stock_movements row, properly comma/quote/newline-escaped), and a demo-data wipe (`lib/settings/demo-data.ts::wipeDemoHouseholdData()`, same table set/order as `scripts/seed-history.ts`'s own wipe, hard-pinned to a fixed `DEMO_HOUSEHOLD_ID` constant plus a runtime `is_demo=true` re-check -- can never target the real household). `notify_email` write closes E-13/S-32's documented activation gap: empty input normalizes to `null` on write (never stores `''`), a simple regex rejects syntactically-invalid input server-side. `monthly_budget` rejects negative values server-side. currency/timezone deliberately NOT exposed (hardcoded app-wide, `lib/date.ts`). Advisor consulted before writing any code -- caught the DEMO_HOUSEHOLD_ID-into-a-server-only-module refactor I'd planned (would have broken `npm run seed:history`/`validate:predictions` at runtime, both scripts run under plain tsx with no `server-only` boundary); kept as a documented second literal instead of a shared import. Also caught the PostgREST 1000-row export-truncation risk (paginated with `.range()`) and the CSV-escaping requirement (item/merchant names are free LLM text -- can contain commas/quotes/newlines). | `app/settings/page.tsx`, `app/settings/actions.ts`, `app/settings/settings-form.tsx`, `app/settings/data-tools-panel.tsx`, `lib/settings/data.ts`, `lib/settings/demo-data.ts`, `lib/settings/export.ts`, `app/page.tsx` (Settings link) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, `/settings` registered dynamic. **Live Supabase, real household:** `updateHouseholdSettings` -- negative budget rejected, invalid email rejected, household row confirmed byte-identical before/after both rejected writes, valid write round-tripped, empty-string email confirmed normalized to `null` (not stored as `''`), reverted to baseline afterward (confirmed identical). `getExportJson`/`getExportCsv` -- JSON parses (238 catalog_items, 22 stock_movements, 2 receipts, 0 price_history at the time of the run), CSV header matches exactly, zero demo-household rows leaked into either export (checked against the real demo `catalog_items` id set). CSV escaper unit-verified directly (comma, embedded quote, newline, plain text, null, number -- all six cases correct) since no real data in this household happened to contain a comma/quote to exercise it naturally. Demo wipe -- snapshotted both households' row counts across all 6 tables before, ran the real wipe, confirmed the real household's counts were byte-identical after and every demo-household table was 0, then reseeded via `ENABLE_SEED=true npm run seed:history` (restored 70 catalog_items/1136 stock_movements/1136 price_history, matching the pre-wipe snapshot exactly). Page-render check (`next dev` + signed gate cookie + curl `/settings` and `/`): all four cards render, household form fields carry the real current values (`VPF1201-Test`, `null` budget, `null` notify_email), the "Manage catalog" link points at `/catalog`, Today's new "Settings" link renders and resolves. | b1f4ac9 | done | (1) Incidental finding, not a regression: the demo household's `item_stats`/`item_stats_history` held 67 stale rows each before this test that neither `seed-history.ts` nor `validate-predictions.ts` ever write (both are explicitly in-memory-only per their own comments) and the nightly recompute-stats cron only ever touches the real household -- no current code path repopulates those two tables for the demo household. The wipe correctly cleared them (same defensive clearing the seeder's own `wipeDemoData` already does, "in case a prior manual run touched them") and reseeding did not restore them, since nothing in this codebase is designed to. Left at 0 post-epic; this is the documented-correct state, not damage, but flagged since the count differs from what existed before this session touched the demo household. (2) The demo-data wipe button has no confirmation UI beyond a browser `confirm()` -- acceptable for a one-operator app acting on a non-production dataset, per the story's own out-of-scope note. |
| S-34 | REQ-25: `lib/date.ts::currentKolkataDayRange()` (day-granularity sibling of the existing `currentKolkataMonthRange`), `lib/receipts/guard.ts` (`DAILY_INGEST_HARD_STOP=100`, `DAILY_INGEST_ALERT_THRESHOLD=50`, `getTodayReceiptCount()`, pure `allowedCountInBatch()` boundary helper), wired into both `app/add/actions.ts` upload Server Actions -- today's count fetched ONCE per batch (not per file), incremented locally as files are accepted, checked strictly before any hashing/Storage/dedup/LLM work so a blocked file costs nothing. `lib/settings/cost-meter.ts::getCostMeterSummary()` (month-to-date via the existing `currentKolkataMonthRange`, only receipts with non-null `parse_cost` count towards spend/average so manual-entry-fallback receipts never dilute it toward zero) rendered on the same Settings page S-33 built: total spend, per-receipt average, count by model tier, today's ingestion status with a plain/amber/red banner at the 0/50/100 boundaries. Advisor-caught: `parse_cost` is USD (`lib/llm/cost.ts`) while the rest of the app renders `monthly_budget` in ₹ -- kept a separate `usd()` formatter (4 decimal places, since per-receipt costs are fractions of a cent and `toFixed(2)` would render `$0.00` for every real value, looking "working" while showing nothing) rather than reusing Insights' `money()` helper, and never rendered the two figures adjacent in a way that could read as the same currency. Also caught: had to enumerate every real call site of `runExtraction`/`createIngestJob` before wiring the guard (found via grep, confirmed by reading `app/add/paste-text.tsx`'s pasted-text path -- it feeds the same client `handleFiles()` pipeline as `capture.tsx`, so `uploadReceiptsAction`/`uploadGroupedReceiptAction` are the only two entry points, not three). | `lib/date.ts`, `lib/receipts/guard.ts`, `app/add/actions.ts`, `lib/settings/cost-meter.ts`, `app/settings/page.tsx` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. Throwaway `npx tsx` script (deleted, never committed; `server-only` temporarily disabled in the touched files for the run, confirmed restored via `git diff` -- zero real diff, only the repo's known pre-existing CRLF noise -- before committing): `currentKolkataDayRange` boundary-verified including the near-midnight-UTC/Kolkata-day-mismatch case the existing month-range function's own comment warns about; `allowedCountInBatch` boundary-verified (countSoFar=99/batch=3 -> 1 allowed; countSoFar=0/batch=5 -> 5 allowed; countSoFar=100/batch=1 -> 0 allowed; countSoFar=105 (over-cap) -> 0 allowed, never negative). **Live Supabase, real household:** `getTodayReceiptCount()` matched a raw count query exactly at baseline (0) and after inserting 100 fixture `receipts` rows dated today (100, confirmed `>= DAILY_INGEST_HARD_STOP`); all 100 fixtures deleted by their captured ids afterward, count confirmed back at baseline (0). `getCostMeterSummary()` matched a hand-computed aggregation over the real month's receipts exactly (both were 0 at the time of the run -- the real household had no receipts with a non-null `parse_cost` in the current Kolkata month); confirmed the 100 null-`parse_cost` fixtures did NOT pollute the total (`totalSpendUsd`/`receiptCount` unchanged with them present). `avgCostPerReceiptUsd` confirmed never `NaN` at 0 receipts. Page-render check confirmed the cost meter card renders the real numbers and "0 receipts uploaded today" (the real current, unblocked state). **Scope note:** the actual guarded Server Actions (`uploadReceiptsAction`/`uploadGroupedReceiptAction`) were verified by direct code read plus the pure `allowedCountInBatch` boundary test rather than an end-to-end live call, since exercising them live would require either uploading 100 real receipts (real Storage + LLM cost, wasteful) or temporarily disabling `server-only` across several unrelated pre-existing files (`lib/receipts/{storage,dedup,ingest}.ts`, `lib/llm/extract.ts`) outside this epic's diff -- judged higher-risk than the data-source-level verification actually performed. | b1f4ac9 | done | (1) The Server Action wiring itself (not just its data sources) was verified by code read + unit test rather than a full live end-to-end call, per the scope note above -- the guard's data source (`getTodayReceiptCount`) and boundary math (`allowedCountInBatch`) are both live/unit-verified individually, and the wiring between them in `app/add/actions.ts` is a single straightforward `if` comparison, but the full path was never exercised with a real multipart upload at count=100. (2) No dollar-cost cap exists, only the 100/day receipt-count cap -- consistent with ADR-0003/PRD, not a gap. |
