| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| Epic-gate fix (S-35, not a new story) | Advisor caught two real issues at the epic quality gate, both fixed before PR: **(1)** `app/api/share-target/route.ts` validated files by filename extension only (`extensionOf(file.name)`) -- Android's share sheet gives no guarantee a shared file's name carries a usable extension (often absent or generic), so a validly-shared photo could silently fall through to the "0 accepted, redirect to /add" path with no explanation. Fixed with the same fallback S-26's clipboard-paste path already established: `extensionOf(file.name) ?? extensionForMimeType(file.type)`. `lib/receipts/formats.ts`'s `EXTENSION_BY_MIME` map gained `'application/pdf': 'pdf'` (additive only -- the one existing caller, `app/paste-listener.tsx`, never sees PDFs, so its behavior is unchanged). **(2)** `public/sw.js`'s install handler used `cache.add(url)` per asset -- `Cache.prototype.add` has thinner browser support than `fetch`+`cache.put`, so on a browser where it's unavailable every asset would silently fail (already caught by the per-asset `.catch`) while still reporting a "successful" install, with no way to detect the resulting empty cache from a check run against a real server. Rewired to explicit `fetch(url).then(r => r.ok ? cache.put(url, r) : throw).catch(...)`. | `lib/receipts/formats.ts`, `app/api/share-target/route.ts`, `public/sw.js` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Unit-verified** (throwaway `npx tsx` script, deleted, never committed) the exact extension-resolution composition the route now runs, across 6 boundary cases: a normally-named file, an empty filename, a generic Android-style share name with no extension, a PDF with no extension, an unsupported MIME with no extension (must fail closed -- confirmed `null`/rejected, not silently accepted), and a named file with an extension that disagrees with its MIME type (filename correctly wins) -- all 6 passed. **Live HTTP-level confirmation, zero additional cost** (per advisor's guidance not to re-pay for a second live Gemini round-trip when the unit proof plus a routing-level check is sufficient): POSTed an unsupported-MIME, unnamed-in-effect file through `/api/share-target` against `next start` -- correctly redirected to `/add` (rejected, no receipt created), confirming the route's wiring end-to-end without incurring cost. Did not re-run the full accept-and-extract path live a second time -- already live-verified once under S-35's own row below at real (negligible, disclosed) cost, and this fix doesn't change that path's behavior for a file that already had a resolvable extension. | (pending -- part of S-35's commit range) | done | None outstanding -- the MIME-fallback gap is closed and unit-proven at its exact boundary; the SW cache-population fix removes a failure mode this repo has no way to directly observe from an agent session (no browser), so the fetch+put rewrite is a lower-surface-area, more broadly-supported primitive rather than a claim the original bug was reproduced. |
| S-35 | REQ-27: PWA manifest (`public/manifest.webmanifest`) with SVG icons (`public/icons/{icon,icon-maskable}.svg` -- no raster-image dependency in this repo, modern Chrome/Android accept `sizes:"any"` SVG manifest icons for installability), a hand-rolled minimal service worker (`public/sw.js`, no Workbox/next-pwa) that precaches only the static shell (manifest/icons/`public/offline.html`) with per-asset tolerated failures (not `cache.addAll`'s all-or-nothing), and only ever intercepts navigation-mode fetches with a network-first/offline-fallback strategy -- never caches or serves a response for any data/API/RSC request. `app/register-service-worker.tsx` registers it client-side. Android Web Share Target: `share_target` in the manifest points at new `app/api/share-target/route.ts`, which reuses the exact same `uploadReceiptFile`/`findDuplicateReceipt`/`createIngestJob`/`runExtraction`/`getTodayReceiptCount`/`allowedCountInBatch` primitives `uploadReceiptsAction` calls (not the Server Action itself -- a Web Share Target POST is a real top-level browser navigation, not a React Server Action call), then redirects (303) to `/review/<id>`. `proxy.ts` exempts exactly four static paths (`/manifest.webmanifest`, `/sw.js`, `/offline.html`, `/icons/*`) from the passcode gate -- **advisor-caught**: a manifest/service-worker fetch is not guaranteed to be a credentialed same-origin request the way a normal page navigation is, so gating these would make "installable" and "offline shell" both silently false while every locally-cookied check still passed. Documented as the same class of exemption as the pre-existing `favicon.ico` carve-out (zero business data, static branding assets only) in both the code comment and this ledger. `app/api/share-target` itself stays fully gated -- no exemption added there. Also advisor-caught: the SW's `fetch(...).catch(() => caches.match(...))` would resolve to `undefined` and throw on a cold-cache offline miss -- fixed with an explicit `Response` fallback. | `public/manifest.webmanifest`, `public/icons/icon.svg`, `public/icons/icon-maskable.svg`, `public/sw.js`, `public/offline.html`, `app/register-service-worker.tsx`, `app/api/share-target/route.ts`, `proxy.ts`, `app/layout.tsx` (manifest link, viewport/theme-color, mounts RegisterServiceWorker -- also carries S-36a's dark-mode script, same file) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/ proxy.ts` -> pass; `npm run build` -> clean, `/api/share-target` registered. **Production-mode (`next build && next start`), no-cookie:** `/manifest.webmanifest`, `/sw.js`, `/icons/icon.svg`, `/offline.html` all 200; `/` and `POST /api/share-target` both still 401 (gate exemption is scoped exactly as intended, no bypass). **With a locally-signed gate cookie** (same HMAC-SHA256 algorithm as `lib/gate.ts`, real `GATE_COOKIE_SECRET`): every real route 200s, `/nonexistent-route` 404s. **Live end-to-end share-target test against the real household/Supabase:** POSTed a real 1x1 PNG through `/api/share-target` -- got a 303 to `/review/<id>`, confirmed a genuine `receipts` row (`status: parsed`) and `ingest_jobs` row (`state: done`) were created, extraction genuinely ran (real Gemini call, ~$0.0035 cost, negligible but real and disclosed rather than hidden). Re-submitted the identical bytes -- correctly redirected to `/add` with zero new rows (duplicate rejected via the same `content_hash` path as manual upload). Deleted the fixture receipt/ingest_job/storage object afterward; confirmed both back to null/removed. | 4f6d7ca | done | SVG-only icons (no PNG) is a deliberate, documented tradeoff (no raster-image tooling in this repo) rather than a gap -- flagged as a residual risk in the final report, not silently accepted. Real on-device install/share-sheet testing (Chrome's actual install prompt, Android's share sheet listing the app) is unattended-agent-unreachable -- verified by production-mode reachability + a live end-to-end share-target call instead, per this story's own documented out-of-scope. An unauthenticated share attempt (no gate cookie yet) 401s with no redirect path back to `/gate` -- consistent with every other unauthenticated request in this app, not a gap unique to this route (would require special-casing the gate, which A26's "returns 401" acceptance doesn't allow for any route). |
| S-36b (branch-time portion) | REQ-26: anon-key/build check + local-401 check, run on this branch before the PR (production sweep runs post-merge -- **advisor-caught**: running the "production URL" sweep pre-merge would test the *old* deployed build, before this epic's own manifest/share-target/boundary changes exist there; splitting the story this way is a deliberate, documented deviation from a literal single-pass reading of the acceptance text, not a shortcut). Confirmed (again, prior grep in the epic's initial context-gathering already found zero references to `NEXT_PUBLIC_SUPABASE_ANON_KEY` anywhere in `app/`/`lib/`/`components/` -- every Supabase call goes through `lib/supabase/server.ts`'s `createServiceClient()`, server-role-key only) that the literal env value is absent from the actual deployable output. | n/a (verification only, no source change) | `npm run build` (clean) then `grep -rl "<anon key>" .next/static/` and `.next/server/` -- **zero matches in both** (the only matches anywhere under `.next/` were in `.next/dev/cache/turbopack/*.sst`, `next dev`'s local Turbopack persistent cache -- never part of `next build`'s deployable output, never served to a browser; confirmed and distinguished explicitly rather than reporting a false positive off an unqualified `.next/`-wide grep). Local unauthenticated 401 + full authenticated route sweep already captured under S-35's ledger row above (same production-mode `next start` session). | (pending -- final commit is this epic's post-merge close-out) | in_progress | The full A1-A26 production sweep, and the live unauthenticated-401-against-the-real-production-URL re-check, run after this epic's PR merges and Vercel deploys the merge commit -- tracked as the remaining half of this story, not silently dropped. |
| S-36a | REQ-26: `app/error.tsx` (segment error boundary, styled retry card) + `app/global-error.tsx` (root-level fallback with its own `<html>/<body>`, since the real layout never rendered on that failure path) + `app/not-found.tsx` (styled 404) + `app/loading.tsx` (one shared root Suspense fallback -- Next applies it to every route beneath it that doesn't define its own, and none of the app's 13 routes have loading needs distinct from each other, so this satisfies "every screen defines a loading state" without 13 duplicated skeletons). Dark mode: a render-blocking inline `<script>` in `app/layout.tsx`'s `<head>` (not `useEffect` -- a post-hydration toggle would flash the wrong theme first) applies/removes `.dark` on `<html>` from `matchMedia('(prefers-color-scheme: dark)')` plus a live `change` listener; `suppressHydrationWarning` added to `<html>` since the script mutates `className` before React hydrates (**advisor-caught** -- without it React 19 flags the mismatch). No manual toggle UI -- acceptance is "functional", not "user-controllable", and there's no display-preferences surface in this app today. Then fixed the ~13 grep-found call sites (`text-/bg-/border-` + a fixed Tailwind palette shade) across `app/insights/page.tsx`, `app/settings/{settings-form,page,data-tools-panel}.tsx`, `app/review/[id]/review-detail.tsx`, `app/add/capture.tsx`, `app/add/manual/manual-entry-form.tsx`: the plain-error cases (`text-red-600`) were simplified to the existing `text-destructive` token (already dark-aware, already the convention used elsewhere in Settings for its own error case -- consistent fix, not a bespoke one), the rest (success/warning/info, no semantic token exists for those) got explicit `dark:` pairs, including one real contrast bug beyond the original grep list: `bg-blue-50 p-3` had no explicit text color at all, inheriting `text-foreground` -- fine in light mode, but light-blue-on-near-white-in-dark-mode would have been unreadable; added an explicit `text-blue-950 dark:text-blue-100` pairing alongside the background fix. Empty-state and mobile-viewport audit: code-trace across all 13 routes -- 11 already had explicit zero-data handling before this epic; documented per-route in this ledger rather than asserted in bulk (Today: A13 cold-start copy; Inventory: "No items match these filters"; Plan: "No items in this bucket yet"; Shopping List: "Nothing due in this selection right now." / "Generate a list above to get started."; Insights: distinct empty copy on all 5 cards; Review queue: header description covers zero-state, no broken render below it; Catalog: delegated to `CatalogManager`, a client component with its own empty handling; Settings and Onboarding are forms with no "zero data" concept, not a gap). No `<table>` elements and no fixed-pixel `min-width`s beyond the existing responsive `maxWidth` containers + global `overflow-x: hidden` -- no horizontal-overflow risk found. Touch targets are the shadcn `Button`/`Checkbox` component defaults (`h-8`/32px buttons) already used consistently across every page in this app -- below the ~44px comfortable-tap guideline, but a pre-existing, app-wide pattern; redesigning the whole button system is out of this story's scope, not silently ignored. | `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`, `app/loading.tsx`, `app/layout.tsx` (dark-mode script + `suppressHydrationWarning`, committed with S-35), `app/insights/page.tsx`, `app/settings/settings-form.tsx`, `app/settings/page.tsx`, `app/settings/data-tools-panel.tsx`, `app/review/[id]/review-detail.tsx`, `app/add/capture.tsx`, `app/add/manual/manual-entry-form.tsx` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Production-mode (`next build && next start`):** `/nonexistent-route` renders the styled not-found page verbatim (`Page not found` / `There's nothing here` / `Back to Today`), confirmed via response-body grep, not just a 200 status code -- guards against the exact false-positive the root `loading.tsx` Suspense wrapping could otherwise cause (**advisor-caught**: curl-based checks must read full response content, not just status, once every route is Suspense-wrapped). All `dark:` sites re-grepped after the fix -- zero remaining unpaired fixed-shade hits (`text-(red\|green\|blue\|yellow\|amber\|gray\|slate\|zinc\|neutral\|orange)-[0-9]` etc. across `app/`). | 8688b3c | done | Investigated and ruled out during verification, not a real regression: `next dev` (and, confirmed separately, `next build && next start` too) returns `200 OK` with an embedded `NEXT_REDIRECT;replace;...` marker for a plain `curl` GET against a route that calls `redirect()` (e.g. `/onboarding` when already onboarded), rather than a bare HTTP 3xx -- verified this is **pre-existing Next.js 16 App Router behavior**, present identically on the unmodified pre-epic code (`git stash` + re-test), unrelated to this epic's `loading.tsx`/error-boundary additions; real browsers execute the shipped client runtime and resolve it correctly, only a non-JS `curl` GET is affected. No user-facing theme toggle exists -- explicitly out of scope per this story's own spec (system-preference-driven only). Full a11y/contrast audit beyond the dark-mode-incidental fixes above is out of scope (REQ-26's criterion is error/empty/loading/mobile/dark-mode, not general accessibility). |
