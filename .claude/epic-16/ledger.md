| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-37 | Mobile reachability (Settings/Shopping List/Review links), Switch/Radio focus rings, gate cookie dev fix, catalog merge-preview error handling | `app/(app)/page.tsx`, `components/ui/switch.tsx`, `components/ui/radio.tsx`, `app/gate/actions.ts`, `app/(app)/catalog/actions.ts`, `app/(app)/catalog/catalog-manager.tsx` | tsc + eslint clean; manual verification per test-plan.md | 16bc40b, 12fe4ad, 0a4c028, 467dd27 | done | None — all changes are additive/scoped, no shared-file conflicts expected with later stories |
| S-38 | New `MobileTopBar` component + rollout to all 12 app routes, back button on Review Detail/Inventory Detail | `components/ui/mobile-top-bar.tsx` (new), all 12 `app/(app)/**/page.tsx` files | tsc + eslint clean; manual verification per test-plan.md | c7bfb0a | done | Inner-content indentation is off by one level in each wrapped page (Fragment adds nesting without full re-indent) — cosmetic only, tsc/eslint don't flag it; worth a pass if this ever gets a formatter run |
| S-39 | Inventory filter `<details>` collapse, Plan tabs horizontal-scroll + per-item overflow, Catalog sticky merge bar, Review Detail sticky commit bar | `app/(app)/inventory/page.tsx`, `components/ui/tabs.tsx`, `app/(app)/plan/plan-item-actions.tsx`, `app/(app)/catalog/catalog-manager.tsx`, `app/(app)/review/[id]/review-detail.tsx` | tsc + eslint clean; manual verification per test-plan.md | aca7779 | done | Sticky bars use `bottom-16` hardcoded to match TabBar's `h-16` — if TabBar height ever changes these two spots need updating too, not derived from a shared token |
