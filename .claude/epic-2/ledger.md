| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-05 | Capture UI (camera/picker/drag-drop), client preprocessing (HEIC->JPEG, downscale), Server Action upload, private Storage bucket | app/add/*, lib/receipts/*, supabase/migrations/20260729020232_receipts_storage_bucket.sql, next.config.ts | `npm run build` -> pass; full Playwright E2E (file-picker PDF+HTML, drag-drop JPEG, rejected .exe) -> pass, 0 console errors; signed URL resolves 200, public-path URL 400 (private bucket confirmed) | 1cdb593 | done | HEIC decode (heic2any) specifically untested — no real HEIC fixture available; the downscale/canvas path it feeds into is independently verified via the JPEG case |
| S-06 | ingest_jobs + Gemini Flash extraction (LlmProvider interface, cost fast path, Zod schema, escalation-trigger detection) | lib/llm/*, lib/receipts/ingest.ts, app/add/actions.ts, next.config.ts, supabase/migrations/20260729121012_receipt_lines_extraction_fields.sql | `npm run build` -> pass; live Gemini API + real hand-built receipt PDF fixture via full Playwright E2E -> all 8 lines 100% correct vs known source data (A1), line-total sum == order_total exactly; parse_path='text' confirmed (A22); failure-path fixture (mismatched total) -> ingest_jobs 'failed' with correct reason + raw_response, receipts.status stays 'pending', zero orphaned receipt_lines | cd7020c | done | Found+fixed: cost fast path was silently always falling back to multimodal — pdf-parse's worker asset didn't resolve under Turbopack bundling, fixed via serverExternalPackages. receipt_lines schema gap found+fixed (item_name/brand/pack_size/category_slug/extract_confidence were missing, would have silently dropped LLM data). "Rasterize" for scanned PDFs interpreted as "send natively as a file part to Gemini" rather than local rasterization — Gemini's own native PDF understanding handles it, avoiding a heavy new pdfjs+canvas dependency |
| S-07 | Escalation ladder (Flash -> Pro retry with correction note on schema/total-mismatch/confidence failure), manual-fallback raw_response retention | lib/llm/extract.ts | `npm run build` -> pass; live API E2E with a deliberately inconsistent fixture (line items sum 100, printed total 5000) -> Flash fails >5% deviation check, escalates to Pro with correction note, Pro reconciles order_total to match actual lines, succeeds (attempts=2, parse_model=gemini-pro-latest); separately confirmed the "never crash" guarantee via a real API-level failure (see risks) | a55147f | done | Found: Gemini key was free-tier not paid (ADR-0003 requires paid for the no-training guarantee) — Pro had a hard 0-quota limit, confirmed via direct API call, not a rate-limit blip. Flagged to user rather than worked around; billing enabled, re-verified after. The free-tier failure itself also incidentally proved the outer catch-all correctly lands 'failed' with a clear error on genuine API/quota errors (distinct code path from the validation-failure ladder, no raw_response since no response existed to retain) |
