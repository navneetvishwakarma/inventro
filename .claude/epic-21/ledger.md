| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| security | pre-merge review | none (verification only) | 2 live exploit attempts against real users, both blocked -- see below | pending | done | security-agent's pre-merge review of the full E-21 diff raised one claimed CRITICAL blocker: "household_members ships with zero RLS -- a self-service master key that defeats every policy." Investigated and REFUTED with direct evidence, not accepted or patched: the reviewing agent had no shell/grep access in its session (Read/Write/advisor only) and, following file references rather than searching, never discovered supabase/migrations/20260802092000_household_members_rls.sql -- the exact fix, built during E-20's own security review (see E-20 ledger), which revokes all write grants from authenticated/anon and enables RLS with a select-own-row-only policy. Re-verified live against the real database (relrowsecurity=true, authenticated has SELECT only) AND by re-running the agent's own two named exploits against two fresh real users: an unfiltered `select('*')` on household_members returned exactly the caller's own row (not the other user's, not all rows), and a self-insert into the other user's household was rejected with "permission denied for table household_members." Both blocked, both directions of the underlying mechanism tested. The rest of the agent's review (RLS predicate soundness, every createServiceClient() call site, getCurrentHouseholdId/resolveHouseholdContext, storage bucket access, client-controlled household_id in writes) returned no findings and is taken as valid independent confirmation. |
| S-66 | cross-household isolation test (epic gate) | e2e/isolation.spec.ts (extended post-security-review to add a direct household_members probe) | 2 consecutive clean standalone passes + passed within the full 26-test suite run; covers list-view isolation, direct-id-probe denial (both directions), a sanity check that each household can still reach its own item, AND (added after the security review below) a direct household_members probe: unfiltered select('*') as A returns exactly A's own row, and A's self-insert into B's household_id is rejected | pending | done | This is the hard gate on E-21 -- two real households via independent browser contexts/signups, each with a manually-entered item, proving isolation both by list-view absence AND by direct-id-probe denial (the assertion that actually distinguishes "RLS enforces this" from "the UI happened not to render a link to it"). Needed test.setTimeout(90_000) -- two full signup+onboarding+manual-entry cycles sequentially is naturally heavier than every other spec in this suite, not a sign of a real slowdown. The household_members probe closes a real gap the original version had: it exercised isolation on every RLS-protected table EXCEPT household_members itself (the table every other table's policy resolves through) -- previously that protection was verified only by ad-hoc one-off scripts during the E-20 and E-21 security reviews, not enforced by the suite. Now a future migration that re-grants INSERT or drops the read_own_membership policy would fail this test, not just go unnoticed until the next manual review. Uses two raw supabase-js clients signed in directly (bypassing the app UI) since this is a data-layer assertion, not a UI one. |
| S-65 | narrow service-role usage | lib/household.ts (getDefaultHouseholdId deleted), .env.example (DEFAULT_HOUSEHOLD_ID removed; also removed stale GATE_PASSCODE/GATE_COOKIE_SECRET left over from E-20), ADR-0006 + docs/architecture/06-security.md (corrected "two exceptions" claim to the real final list) | build clean, unit 26/26, e2e auth+routes+fidelity 19/25 passed (6 legitimate skips), grep confirms getDefaultHouseholdId/DEFAULT_HOUSEHOLD_ID have zero live references (only historical comments) | pending | done | The real exception list ended up larger than ADR-0006's original "exactly two call sites" claim: cron (2 routes) + seeder (unchanged) + storage bucket access (3 files, storage.objects has zero RLS) + wipeDemoHouseholdData (targets a household the caller isn't a member of) + signup's compensating cleanup (E-20 security fix). Each is legitimately justified and was already documented at the point it was added -- this story's job was reconciling the ADR/security-doc's summary claim with what actually shipped, not re-litigating any of them. |
| S-64 | migrate settings/catalog/onboarding | lib/catalog/{manager,search}.ts, lib/settings/{cost-meter,data,demo-data,export}.ts (onboarding/data.ts already done in E-20) | build clean, unit 26/26, e2e auth+routes+fidelity combined run 20/20 (5 legitimate skips) | pending | done | wipeDemoHouseholdData (settings/demo-data.ts) deliberately stays on service-role -- it always targets the fixed DEMO_HOUSEHOLD_ID, a household the calling user never belongs to, so a request-scoped client would be correctly denied by RLS. This is now a real (if low-severity) residual risk worth naming rather than silently carrying forward: any authenticated user, not just the one household of v1, can trigger it -- target is regenerable synthetic fixture data, never real tenant data. Documented in docs/architecture/06-security.md rather than restricted. |
| S-63 | migrate inventory/plan/shopping-list/insights | lib/inventory/{consume,data}.ts, lib/plan/{actions,data}.ts, lib/shopping-list/{checkoff,data,generate}.ts, lib/insights/data.ts, lib/household.ts (new resolveHouseholdContext/HouseholdContext shared helper), lib/notifications/digest.ts, app/api/cron/{digest-daily,digest-weekly,recompute-stats}/route.ts | build clean, unit 26/26, e2e routes+fidelity 15/15 (5 legitimate skips on the fresh household), new manual-entry e2e still passing, all 3 cron routes verified live via direct curl with the real CRON_SECRET | pending | done | Confirmed the caution flagged in S-63's own spec was real: getDueSoonItems (used by the digest cron) chains through getPlanItems -> getInventoryItems, both of which now require a session -- calling them from cron would have crashed every digest invocation. Also found and fixed the identical bug already live in recompute-stats (getDefaultHouseholdId(), which S-65 deletes). Fixed with a shared HouseholdContext override (resolveHouseholdContext in lib/household.ts) threaded through all three functions, defaulting to the request-scoped session for every real user-facing caller. Rewrote all 3 cron routes to enumerate every household via service-role instead of a single hardcoded id -- the architecturally correct behavior now that more than one household exists, not just an access-pattern fix; verified live against 122 accumulated test households (since cleaned up) plus the two seeded ones. |
| S-62 | migrate capture/review/commit | lib/llm/extract.ts, lib/receipts/{canonicalize,dedup,guard,ingest,storage}.ts, lib/review/{commit,data}.ts, lib/catalog/manual-entry.ts. Also pulled forward from S-63 (recomputeOneItem is called from this file set): lib/predictions/recompute.ts split (recomputeOneItem -> request-scoped, recomputeAllItemsForHousehold stays service-role/cron-only), lib/predictions/reconcile.ts (type widened, already parameter-driven) | build clean, unit 26/26, new e2e test (routes.spec.ts "create a new item and log a purchase end to end") exercises the full write path -- passes, and the created row confirmed via direct DB query to have a real (non-default) household_id | pending | done | Storage bucket access (storage.objects) has ZERO RLS policies by original design (20260729020232) -- access control there is private-bucket + signed-URLs, not per-role RLS. A request-scoped client would get a permission error on every download/upload. Every file with a `.storage.*` call now uses TWO clients: createRequestClient() for table ops (real RLS), createServiceClient() for storage ops specifically -- documented inline in each file, not a silent inconsistency. runExtraction() is invoked via Next's after() (app/(app)/add/actions.ts, app/api/share-target/route.ts) -- confirmed after() preserves request context so cookies()/createRequestClient() work correctly there. |
| S-61 | request-scoped client | lib/supabase/server.ts (createRequestClient), lib/household.ts (getCurrentHouseholdId) | Already covered by E-20's auth e2e suite (5/5) and S-60's live isolation probe, which exercises the same underlying session/client pattern | 5de2fdc (E-20) | done | Pulled forward into E-20/S-57 out of necessity -- signup's redirect into onboarding couldn't work against a static DEFAULT_HOUSEHOLD_ID. No new work this story; recorded here so the epic's own story list stays an accurate record rather than silently skipping it. |
| S-60 | RLS rewrite | supabase/migrations/20260802100000_rls_household_members.sql | Applied clean via `supabase db push`; `pg_class.relrowsecurity` confirmed true on all 13 tables (categories correctly false); live two-household isolation probe via real signups + service-role-seeded catalog_items: A sees only its own item, direct-id probe of B's item returns [], attempted update of B's item affects 0 rows, B's row confirmed unchanged after the attempt | pending | done | households uses `id`, not `household_id`, unlike the other 12 tables -- handled as a separate statement outside the generic per-table loop (would have broken the whole migration if missed). Pre-existing default GRANTs on these 12+1 tables were left as-is (unlike household_members in E-20) -- RLS is the correct narrowing mechanism here since these tables always intended broad authenticated-role access scoped by row, whereas household_members had no RLS at all before that fix. This migration alone changes nothing for the running app yet -- it still uses createServiceClient() everywhere (bypasses RLS), per ADR-0006's own sequencing; S-61-S-64 are what make it matter. |
