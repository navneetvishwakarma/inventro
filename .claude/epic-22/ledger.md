| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-68 | matched lines get the same reassignment control as needs_review lines | app/(app)/review/[id]/review-detail.tsx (new MatchedLineRow, reuses NeedsReviewRow verbatim once expanded; also fixed a pre-existing 'uncategorized' duplicate-Select-option/React-key bug surfaced by this story's own e2e test), e2e/review-states.spec.ts (new test) | tsc clean, eslint clean, e2e review-states.spec.ts 4/4 + fidelity/routes suites unaffected (19 passed, 5 legitimate skips) | pending | done | Reassigning a matched line to a genuinely different catalog item depends on the live trigram-match algorithm's exact score for arbitrary input text -- not reproduced deterministically in e2e; the test instead proves the affordance (click opens the real form) and that an action taken there persists, via "Mark non-inventory" (the simplest of the three available actions, already covered elsewhere for correctness). The 'uncategorized' Select fix was out of this story's own scope but directly adjacent (same component, same file, trivial one-line fix) -- left unfixed it would have silently affected every NeedsReviewRow/NewItemRow render too, not just the new MatchedLineRow path. |
| S-67 | review queue surfaces failed/stuck extractions; block commit on zero-line/non-parsed receipts | lib/review/data.ts (getReviewQueue/getReceiptForReview join ingest_jobs state), app/(app)/review/page.tsx (status badge), app/(app)/review/[id]/review-detail.tsx (ProcessingCard/FailedCard, canCommit zero-line guard), app/(app)/review/[id]/actions.ts (retryExtractionAction), supabase/migrations/20260803090000_commit_receipt_status_and_line_guard.sql (SQL-level status + zero-line guard), e2e/review-states.spec.ts (new) | build clean, tsc clean, eslint clean, unit 26/26, e2e review-states.spec.ts 3/3 (2 consecutive runs) + fidelity/routes suites unaffected; SQL guard verified live via direct RPC calls against real scratch receipts (status=pending rejected, parsed-with-zero-lines rejected) before being wrapped into the e2e spec | pending | done | The commit_receipt() SQL fix is the defense-in-depth half of this story's own invariant -- a disabled Commit button alone doesn't stop a direct RPC call. retryExtractionAction reuses runExtraction() unchanged (same after()-scheduled pattern as a fresh upload) rather than building new retry infrastructure -- safe because a failed attempt never inserts receipt_lines (lib/llm/extract.ts returns before that step), so retrying can't duplicate rows. ingestState is read via a second query (ingest_jobs keyed on receipt_id, no unique constraint) rather than a PostgREST embed, kept simple over an embed's to-one/to-many ambiguity. |
