| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-72 | spec-parity polish pass (P2, 4 bundled sub-items) | app/(app)/plan/page.tsx (running estimated total per bucket), app/(app)/inventory/[id]/item-admin-controls.tsx (new: staple/perishability/cadence-override/archive), app/(app)/inventory/[id]/actions.ts + lib/catalog/manager.ts (setStaple/setPerishabilityDays, new), app/(app)/insights/page.tsx (budget link), e2e/spec-parity.spec.ts (new) | tsc clean, eslint clean, build clean, unit 26/26, e2e spec-parity.spec.ts 3/3 (2 consecutive standalone passes on the item-detail test, which is flaky only when batched with other heavy sequential tests -- the established Chromium/Turbopack resource-pressure pattern, not a regression) + routes/fidelity/confirmations/mobile-back-nav/inventory-empty-states suites unaffected | pending | done | Login's pending-submit-button sub-item was found ALREADY implemented (app/(auth)/login/login-form.tsx and signup/page.tsx both already use useActionState's isPending, disabled inputs, "Logging in.../Creating..." copy) -- no code change, verified and recorded rather than silently skipped. Cadence-override was initially assessed as dead schema (recompute.ts's own comment: "owned by the Plan screen's manual override, a later epic") and nearly split out per this story's invariant -- caught before implementing: that epic already shipped (lib/plan/actions.ts's setCadenceOverride, live on /plan today). Reused it unchanged, but its own real gap surfaced during e2e verification: it writes item_stats.cadence_override, which only exists for items that have already been recomputed at least once -- guaranteed on the Plan page (getPlanItems() filters to cadenceBucket !== null, which implies a row exists) but NOT guaranteed on item-detail, which shows every non-archived item regardless of purchase history. Fixed by scoping the item-detail control to only render when cadenceBucket !== null, the same structural guard Plan already relies on -- not a hack, a correctly-scoped boundary. Archive redirects to /inventory on success since archived items are filtered out of getInventoryItem(), which would otherwise 404 the page the user is still on. Plan's running total reuses avgUnitPrice90d (the same per-purchase-average field item-detail's own "Avg price (90d)" stat already shows) rather than inventing a new price definition -- items without price history are excluded from the sum and counted separately so the total doesn't silently understate itself. |
| S-71 | mobile back navigation for non-tabbed pages | app/(app)/shopping-list/page.tsx, app/(app)/settings/page.tsx, app/(app)/review/page.tsx (each backHref="/"), app/(app)/catalog/page.tsx (backHref="/settings"), e2e/mobile-back-nav.spec.ts (new) | tsc clean, eslint clean, e2e mobile-back-nav.spec.ts 4/4 (2 consecutive runs) + fidelity/routes suites unaffected (19 passed, 5 legitimate skips) | pending | done | Parents chosen from real inbound links (grepped, not guessed), per the spec's own framing of this as a judgment call: Shopping list, Settings, and Review queue are each linked only from Today ("/"). Catalog manager has three inbound sources (Today, Inventory, Settings) -- Settings is the only one with a dedicated, purpose-built entry point (a "Catalog" card with a "Manage catalog" link), so that's the parent, not Today's incidental quick-action or Inventory's "seeing a duplicate?" aside. |
| S-70 | inventory: distinct empty state for cold-start vs. filtered-to-nothing | app/(app)/inventory/page.tsx (branches on items.length, the already-fetched unfiltered list -- no second query needed), e2e/inventory-empty-states.spec.ts (new) | tsc clean, eslint clean, e2e inventory-empty-states.spec.ts 2/2 (2 consecutive runs) + fidelity/routes suites unaffected (17 passed, 5 legitimate skips) | pending | done | The cold-start vs. filtered-to-nothing distinction didn't need a second round trip -- `items` (the unfiltered list) was already fetched and in scope before the filtering happens; `items.length === 0` is cold-start, `items.length > 0 && filtered.length === 0` is a real filter dead-end. Cold-start next-action points at /add rather than reusing the generic 'Clear filters' link, since there's nothing to clear. |
| S-69 | consistent confirmation for destructive/ledger-writing actions | components/ui/confirm-panel.tsx (new shared ConfirmPanel), app/(app)/inventory/[id]/consume-actions.tsx (Used it up/Wasted now confirm), app/(app)/settings/data-tools-panel.tsx (window.confirm replaced), app/(app)/shopping-list/generate-list-panel.tsx (new, replaces inline forms; conditional confirm only when the active list has checked-off items), app/(app)/shopping-list/page.tsx + actions.ts (generateDueInDaysAction deleted, now dead after the client-driven rewrite), e2e/confirmations.spec.ts (new) | tsc clean, eslint clean, build clean, unit 26/26, e2e confirmations.spec.ts 3/3 (2 consecutive runs) + routes/fidelity suites unaffected (15 passed, 5 legitimate skips) | pending | done | Investigated the Catalog MergePanel per the spec's own preference to extend it -- it's welded to a two-item survivor/loser radio + async preview fetch, not a generic confirm shape, so built the minimal shared ConfirmPanel the other three call sites actually need instead of contorting it (spec's parenthetical was a preference, not a hard constraint -- "don't introduce a fourth pattern" was the actual requirement, and this is the second pattern, not a fourth). Shopping-list regeneration confirm is conditional on hasCheckedItems (computed server-side from the active list) -- unconditional would be a confirm-fatigue regression on the common empty/unchecked-list path. The consume-actions e2e test caught a real gap in its own first draft: recordConsumption() silently no-ops on zero stock, so the test seeds a known non-zero stock_movements row directly rather than relying on manual-entry's default quantity. |
| S-68 | matched lines get the same reassignment control as needs_review lines | app/(app)/review/[id]/review-detail.tsx (new MatchedLineRow, reuses NeedsReviewRow verbatim once expanded; also fixed a pre-existing 'uncategorized' duplicate-Select-option/React-key bug surfaced by this story's own e2e test), e2e/review-states.spec.ts (new test) | tsc clean, eslint clean, e2e review-states.spec.ts 4/4 + fidelity/routes suites unaffected (19 passed, 5 legitimate skips) | pending | done | Reassigning a matched line to a genuinely different catalog item depends on the live trigram-match algorithm's exact score for arbitrary input text -- not reproduced deterministically in e2e; the test instead proves the affordance (click opens the real form) and that an action taken there persists, via "Mark non-inventory" (the simplest of the three available actions, already covered elsewhere for correctness). The 'uncategorized' Select fix was out of this story's own scope but directly adjacent (same component, same file, trivial one-line fix) -- left unfixed it would have silently affected every NeedsReviewRow/NewItemRow render too, not just the new MatchedLineRow path. |
| S-67 | review queue surfaces failed/stuck extractions; block commit on zero-line/non-parsed receipts | lib/review/data.ts (getReviewQueue/getReceiptForReview join ingest_jobs state), app/(app)/review/page.tsx (status badge), app/(app)/review/[id]/review-detail.tsx (ProcessingCard/FailedCard, canCommit zero-line guard), app/(app)/review/[id]/actions.ts (retryExtractionAction), supabase/migrations/20260803090000_commit_receipt_status_and_line_guard.sql (SQL-level status + zero-line guard), e2e/review-states.spec.ts (new) | build clean, tsc clean, eslint clean, unit 26/26, e2e review-states.spec.ts 3/3 (2 consecutive runs) + fidelity/routes suites unaffected; SQL guard verified live via direct RPC calls against real scratch receipts (status=pending rejected, parsed-with-zero-lines rejected) before being wrapped into the e2e spec | pending | done | The commit_receipt() SQL fix is the defense-in-depth half of this story's own invariant -- a disabled Commit button alone doesn't stop a direct RPC call. retryExtractionAction reuses runExtraction() unchanged (same after()-scheduled pattern as a fresh upload) rather than building new retry infrastructure -- safe because a failed attempt never inserts receipt_lines (lib/llm/extract.ts returns before that step), so retrying can't duplicate rows. ingestState is read via a second query (ingest_jobs keyed on receipt_id, no unique constraint) rather than a PostgREST embed, kept simple over an embed's to-one/to-many ambiguity. Advisor flagged retryExtractionAction as a new public Server Action taking a bare receiptId with no explicit ownership check -- investigated rather than assumed safe: verified live with two real households that runExtraction()'s first step (a request-scoped, RLS-bound `.single()` lookup of the receipt) returns a PostgREST "cannot coerce to single object" error for a foreign household's receipt id (zero rows match the RLS predicate), and the function returns immediately on that error before ever touching the service-role storage client or writing ingest_jobs. RLS on the initial lookup is the effective ownership gate; confirmed, not just assumed from reading the code. |
