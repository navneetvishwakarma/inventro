| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-14a | Pure computeItemStats steps 1-5: gather/merge same-Kolkata-day purchase events (24mo window, 40-event cap), intervals, outlier rejection, EWMA (alpha=0.35), shrinkage to category prior (w=n/(n+2)) | lib/predictions/types.ts, lib/predictions/computeItemStats.ts | `npx tsc --noEmit` -> pass; throwaway tsx fixture script (deleted) -> clean weekly sequence ewma/interval_est=7 exact, same-day merge collapses 2 same-Kolkata-day events into 1 (summed qty, no spurious ~0d interval), sub-0.5d interval dropped, outlier rejection engages and correctly drops [7,7,60]->[7,7] at n=3 usable intervals, does NOT engage at n=2 ([7,60] both retained, proving the lowered gate is a real gate not a no-op), shrinkage weight at n=1 matches hand-computed w=1/3 blend | 33596d6 | done | Outlier-rejection gate deliberately lowered from the working spec's literal n>=5 (step 3) to n>=3 -- advisor-caught before implementation: the literal gate disables rejection at exactly the data volume the rule's own stated purpose (one vacation/bulk-buy) targets, and a strict n>=5 reading breaks acceptance A6 (day 0/7/60/67, only 3 intervals). Verified by hand: under n>=5, A6's 60d gap survives into the EWMA and pushes the bucket to fortnightly, contradicting A6's required outcome. Backlog.json's S-14a acceptance text updated to n>=3 during define-epic, not left silently diverging from the working spec |
| S-14b | Extend computeItemStats with steps 6-7: q-gated (>=70% reliable qty) trailing-120d daily_rate_base, depletion-date blend into next_purchase, perishability clamp | lib/predictions/computeItemStats.ts | `npx tsc --noEmit` -> pass; throwaway tsx fixture script (deleted) -> q>=0.70 fixture computes dailyRateBase (~32.6 g/day, hand-verified) and a depletion date ~15d out matching 500g/32.6 g/day; q<0.70 fixture leaves dailyRateBase null and next_purchase falls through to exactly lastPurchasedAt+intervalEstDays (q=0 limit); a single in-window event (q=1 overall) still suppresses the rate branch for lack of window data; perishability clamp binds a 2-day cap against a rate-based estimate that would otherwise run ~15d, and also fires standalone (depletion set directly from lastPurchasedAt+perishabilityDays) when the rate branch didn't run at all | 44c450c | done | q is computed over the full step-1 (pre-outlier-rejection) event set, not the post-rejection interval count -- a deliberate choice (data-quality-of-quantity and interval-outlier-rejection are orthogonal axes), documented in sub-S-14b.json's invariants, not implied anywhere in the working spec text |
| S-14c | Complete computeItemStats (steps 8-10: confidence n/(n+3), cadence bucket thresholds, hysteresis with unpredictable-transitions unhysteresised), recompute.ts (fetch inputs, call computeItemStats, upsert item_stats + insert item_stats_history), migration for interval_est_days + trim_item_stats_history() | lib/predictions/computeItemStats.ts, lib/predictions/recompute.ts, supabase/migrations/20260730080000_item_stats_interval_est_and_history_trim.sql | `npx tsc --noEmit` -> pass; throwaway tsx fixture script (deleted) -> A5 (day0/7/14/21) bucket=weekly, confidence exactly 0.5, next purchase exactly 7d out; A6 (day0/7/60/67) outlier rejected, stays weekly; A7 (2 purchases) interval_est=23.33 (hand-verified 1/3*10+2/3*30), confidence exactly 0.25, bucket=unpredictable; hysteresis no-flap at 10.5 vs boundary 10 (5% over, stays weekly), real flip at 12 (20% over, moves to fortnightly), no hysteresis on first-ever compute; live-Supabase integration script (deleted, server-only temporarily disabled per repo convention and restoration confirmed via `git diff`) -> real recompute produces a populated item_stats row + exactly one history row, 6 consecutive recomputes cap history at 5, a manually-set cadence_override survives recompute untouched, a zero-purchase item writes no item_stats row | a43e0e7 | done | Confidence formula's own worked example (A5 "confidence High") does not hold under the literal n/(n+3) formula with n=3 (computes to exactly 0.5, Medium) -- advisor-confirmed resolution: keep the formula (algorithm detail, AGENTS.md's tiebreak), correct A5's label to Medium in backlog.json/GH issue during define-epic. Unpredictable-bucket transitions are exempt from hysteresis (two independent triggers -- interval>500 OR confidence<0.35 -- share no single numeric boundary a >15%-crossing test applies to); scoped explicitly in sub-S-14c.json rather than silently gapped |
| S-15 | Repurchase reconciliation loop: reconcileRateCorrection(supabase, catalogItemId, thisPurchase) compares the previous recompute's own predicted_depletion_at/daily_rate_base against the actual repurchase date, adjusts rate_correction (x0.85 / x1.15), clamped [0.5, 2.0]; eligibility (chronologically-latest-purchase) determined from stock_movements, never item_stats; gated on at least one real consumption/adjustment/waste signal existing for the item (fix, see risks) | lib/predictions/reconcile.ts | `npx tsc --noEmit` -> pass; throwaway live-Supabase integration script (deleted, server-only temporarily disabled per repo convention, restoration confirmed via `git diff`) -> Test A (remaining projected stock >40% of pack) fires x0.85; Test B (projected stock gone >20% of interval) fires x1.15; neither fires when depletion lands close to the real purchase date; 10 repeated Test-A fires clamp exactly at the 0.5 floor (test redesigned mid-verification to hold dailyRateOld constant across iterations, isolating the hard clamp from the reconciliation's own natural self-limiting feedback -- see risks); a backdated commit (a later purchase already on record), a first-ever purchase (no prior movement), and a missing default_pack_size all skip cleanly (rate_correction unchanged). FIX verification (post-epic-quality-gate advisor pass, before shipping): a fresh live-Supabase reproduction script ran 10 sequential real commits (recomputeOneItem + reconcileRateCorrection, 1000g purchases 30 days apart, zero consumption movements, real stock_epoch temporarily backdated to fit the window then restored) -- rate_correction floor-walked to 0.85 by purchase 6 and continued descending under the pre-fix code, confirming the bug empirically rather than by inspection alone; after the guard, the identical repro holds rate_correction at 1 for all 10 purchases. Re-ran all 8 original S-15 scenarios (Test A/B/neither/clamp/3 skips) with a token consumption movement added to each so the new guard doesn't mask the threshold logic under test, plus 2 new guard-specific cases (no signal skips; signal present still fires) -- all 10 pass | 3c8327b (original), fix commit TBD | done | Advisor caught two issues before implementation: (1) a raw ledger-sum baseline for "projected stock" would grow monotonically and walk rate_correction to the floor every cycle -- fixed by baselining against the previous recompute's own item_stats.predicted_depletion_at/daily_rate_base instead; (2) item_stats.last_purchased_at is unusable for the "latest purchase" eligibility check since commit_receipt() (S-13) has already advanced it to THIS purchase by the time reconcile runs -- fixed by querying stock_movements directly. A third bug found DURING this story's own live-Supabase verification (not by advisor, by running the test and reading the wrong result): the eligibility query's `.neq('source_receipt_id', ...)` silently dropped stock_movements rows where source_receipt_id is NULL, under Postgres's three-valued NULL-comparison logic. Fixed with a null-safe `.or()` filter. **FOURTH, more serious bug caught by a second advisor pass at the epic quality gate (after all 5 stories had already landed and `npm run build` was clean):** the first fix only moved WHERE the monotonic-growth baseline was read from, not removed the growth itself -- predicted_depletion_at is anchored on v_current_stock, a raw ledger sum that only decreases via explicit consumption/adjustment/waste movements (F9.1/F9.2, E-7, not built yet), so for any repeatedly-purchased item with no consumption ever logged, predicted_depletion_at drifts further into the future every cycle regardless of baseline source, and the >40%-of-pack branch fires on nearly every repurchase -- reproduced empirically (see tests column) before being trusted as real. Fixed by gating reconciliation entirely on the existence of at least one non-purchase stock_movement for the item; without one, rate_correction is left unchanged. This makes reconciliation a documented no-op on real households until E-7 ships consumption logging -- a genuine scope dependency, not a workaround, recorded in sub-S-15.json's invariants. A FIFTH bug, introduced while writing the fourth fix and caught by re-running the full test suite immediately after (not by advisor): the new consumption-signal query was inserted into the middle of a `Promise.all([...])` array without updating the destructuring order, silently binding `prevPurchaseRes` to the wrong query's result -- caught because the "consumption signal present" guard test failed unexpectedly, not because anything looked wrong on inspection. Also fixed in this same pass: `computeItemStats.ts`'s 24-month window filter and 120-day rate-window filter compared ISO timestamp strings lexicographically instead of numerically (same bug class as E-4's Kolkata-midnight issue) -- switched to `getTime()` comparison throughout; a live-Supabase-review self-pass on the touched `proxy.ts` matcher (required since this diff touches an auth-adjacent path) found no exploitable issues, confirmed the `api/cron/` exclusion is scoped exactly to that prefix |
| S-16 | On-commit trigger (lib/review/commit.ts, after commit_receipt() succeeds: reconcileRateCorrection then recomputeOneItem per affected item) + nightly Vercel Cron (recomputeAllItemsForHousehold, batched reads/writes, app/api/cron/recompute-stats/route.ts, vercel.json 03:00 IST, CRON_SECRET auth, proxy.ts matcher exclusion) | lib/review/commit.ts, lib/predictions/recompute.ts, app/api/cron/recompute-stats/route.ts, vercel.json, proxy.ts, .env.example | `npx tsc --noEmit` -> pass; throwaway live-Supabase integration script (deleted, server-only temporarily disabled across the commit.ts/canonicalize.ts/supabase-server.ts/reconcile.ts/recompute.ts/household.ts chain, restoration confirmed via `git diff` on every file) -> a synthesized commit-ready receipt (2 matched + 1 new_item line, bypassing capture/extraction to isolate S-16's own logic) produces item_stats for all 3 items immediately after commitReceipt() returns (synchronous, no queue); a subsequent nightly sweep recomputes them again and correctly skips a zero-purchase item (no row, no error); separate throwaway script invoking the route handler directly -> missing/wrong/unset CRON_SECRET all 401 (fails closed), valid secret 200; proxy.ts's updated matcher regex verified against sample paths (excludes api/cron/*, still covers a normal route and the /gate exclusion) | 5391951 | done | Deviation from the epic prompt's initial mention of pg_cron: uses Vercel Cron instead, per advisor confirmation -- matches the already-approved ADR-0005/02-tech-stack.md/05-api-design.md contract (`POST /api/cron/recompute-stats`, nightly 03:00 IST); pg_cron can't reach the pure-TypeScript computeItemStats without pg_net, which the approved architecture doesn't use. Residual: CRON_SECRET must also be set in the Vercel project's environment for the deployed cron to authenticate, and actual Hobby-tier invocation timing is unverified from this session (no Vercel CLI auth available). A commit_receipt() success followed by a recompute failure (e.g. a transient DB error) is allowed to throw and surface as a commitReceiptAction failure even though the receipt is, in fact, already committed -- not swallowed/retried, since the nightly cron self-heals any missed recompute the next cycle and swallowing would hide a real bug; noted as a deliberate simplicity choice, not an oversight |
