| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-17 | Deterministic (seeded PRNG) synthetic-purchase-history generator: ~70 catalog items across the 7 working-spec Sec12 cohorts (clean_periodic 25, high_variance 10, outlier_injected 8, drifting 8, cold_start 10, perishable_implausible_rate 5, qty_inconsistent 4), seeded into a dedicated is_demo household via `npm run seed:history` (ENABLE_SEED=true gate, idempotent reseed, `--wipe` mode); write guard re-asserts is_demo=true + id!=DEFAULT_HOUSEHOLD_ID before every insert/delete; additive migration making stock_movements.qty_base nullable | scripts/seed-history/plan.ts, scripts/lib/supabaseAdmin.ts, scripts/seed-history.ts, supabase/migrations/20260730090000_stock_movements_qty_base_nullable.sql, package.json (tsx devDependency, seed:history script) | `npx tsc --noEmit` -> pass; `npx eslint scripts/` -> pass; throwaway tsx fixture script (deleted) -> generateItemDefs() deterministic across two calls (byte-identical), 70 items with correct per-cohort counts, all canonical_names unique, weekly item gaps ~7d capped at the algorithm's own 40-event cap, drift interval goes ~7d (oldest) -> ~14d (most recent), outlier lands at the planned index, qty_inconsistent's deterministic even-spacing dropout gives exactly q=0.5 (4/8 null) -- replaced an earlier per-event-Bernoulli version that could land q on the wrong side of the 0.70 gate purely by chance at n=8; live-Supabase run (via seed-history.ts itself, not a throwaway script since this IS the deliverable) -> wiped + seeded the demo household: 70 catalog_items, 1136 stock_movements, correct per-cohort counts, idempotent reseed and `--wipe` mode both confirmed against the live linked project | 62889de | done | Migration adds nullable qty_base to stock_movements -- not anticipated in the original sub-S-17.json spec text (written before the qty_inconsistent cohort's DB-level requirements were fully worked out during implementation); necessary because a real NULL is required to exercise computeItemStats' `e.qtyBase !== null` q-gate against actual DB-read data (a sentinel like 0 would be silently counted as "reliable" and would also corrupt the sum). Additive/safe: no existing rows are null, commit_receipt() (S-13) always writes a concrete value per S-10's acceptance, and only the demo household is ever written to. A per-request bulk `stock_movements` fetch (all ~1100+ demo rows in one `.select()`) silently truncates at Supabase/PostgREST's default 1000-row cap -- caught by S-18 during validation (see below), not by this story's own tests, since S-17 never reads movements back in bulk itself (only inserts, chunked 500/batch, which has no such cap). |
| S-18 | Held-out accuracy scorecard (`npm run validate:predictions`): trains computeItemStats on all-but-the-last purchase per seeded item, holds out the last as ground truth, no persistence; S3 inclusion gate >=4 training events (>=5 total); prints the Sec12-format per-cohort table + overall S3 (n>=4) PASS/FAIL vs 70%; per-cohort qualitative checks (confidence bands, outlier rejection via `_internal.rejectOutliers`, perishability clamp on predictedDepletionAt only, qty-inconsistent rate suppression) | scripts/validate-predictions.ts, lib/predictions/computeItemStats.ts (computeRateCrossCheck fix), package.json (validate:predictions script) | `npx tsc --noEmit` -> pass; `npx eslint scripts/ lib/predictions/computeItemStats.ts` -> pass; `npm run build` -> clean; throwaway tsx fixture script (deleted) -> reproduced all three E-5 headline fixtures post-fix (A5: bucket=weekly, confidence=0.50 exactly, next purchase=day 28 exactly against a weekly-matched prior; A6: 60d outlier rejected, stays weekly; A7: intervalEst=23.33, confidence=0.25, bucket=unpredictable) -- unaffected by the rate-cross-check fix since all three run at q=0 (qtyBase omitted), which returns before the modified line; isolated 2-purchase/90-day-apart fixture hand-verifying the bug (old formula: dailyRateBase=2000/90=22.2/day, predicted depletion at 45d against a true 90d interval) and the fix (1000/90=11.1/day, exactly 90d); live-Supabase run against S-17's seeded demo household -> first run surfaced the Supabase 1000-row bulk-fetch truncation (found by comparing this script's own per-item movement counts against the seeder's known plan counts -- exactly the "harness-bug-first" step of the tuning protocol), fixed by switching to per-item queries (10-way concurrent batches, matching lib/predictions/recompute.ts's fetchInputs() query shape); second run (harness fixed, algorithm not yet) surfaced S3=53%/65% and a systematic ~50% under-prediction on 60-120-day-cadence items, hand-diagnosed via a controlled fixture (not assumed) to be a real computeRateCrossCheck double-counting bug, not a harness or seeder-jitter issue; third run (post-fix) -> S3 (overall, n>=4) = 78% [target 70%] PASS, 60 items scored: clean_periodic 92% (25 items, matches the working-spec Sec12 88% anchor for sigma=0.15), high_variance 40% (10 items, expected -- sigma=0.40 cohort; confidence correctly reads non-High for 100%), outlier_injected 63% (8 items; bucket held despite the injected outlier for 100%, the outlier itself rejected for 88%), drifting 75% (8 items), perishable_implausible_rate 100% (5 items; predictedDepletionAt clamp holds for 100%), qty_inconsistent 100% (4 items; dailyRateBase correctly null for 100%); cold_start (10 items, all n<4) correctly excluded from S3, confidence=Learning for 100% | 7a8de3f | done | **Real algorithmic defect found and fixed, not a harness issue or one of the 4 named tunable constants (EWMA_ALPHA/shrinkage/OUTLIER_MAD_MULTIPLIER/HYSTERESIS_MARGIN):** computeRateCrossCheck (S-14b) summed ALL purchase quantities inside the trailing 120-day window, including the most recent (not-yet-consumed, still-on-the-shelf) purchase itself -- over k window events spanning k-1 real intervals, this counts k packs' worth of consumption instead of k-1, biasing dailyRateBase high by a factor of k/(k-1). Worst (a flat 2x) at k=2, which is the common case for any 60-120 day cadence item (quarterly items were predicting depletion at ~50% of their true interval, a serious real-world defect for staples like rice/detergent/LPG bought on that cadence). Fixed by excluding the window's most recent event from the sum (semantically: that quantity hasn't been consumed yet -- it IS the current stock). Advisor consult was attempted before deciding to fix vs. only document, but the advisor tool was temporarily overloaded at that moment; proceeded on first-principles derivation (worked through both "exclude first" and "exclude last" algebraically, confirmed only "exclude last" is correct once purchase quantities vary, not just in the idealized equal-quantity synthetic fixtures) plus empirical reproduction (isolated fixture) and full re-verification of every E-5 acceptance fixture before trusting the change, matching this story's own tuning protocol. Residual: the fix's correctness for the on-commit path (S-16) is exact (now == the last purchase's own timestamp, by construction); for the nightly-cron path (recomputeAllItemsForHousehold), "now" is wall-clock cron time, which can be meaningfully after the last purchase for a slow-cadence item -- in that case treating the most recent purchase as "fully unconsumed" is a slight (second-order) over-simplification, not incorrect, and not addressed further here (out of E-6's scope; the bug this story found and fixed was first-order and dominant). |
