| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-19 | `/inventory` (grouped by leaf category, "Out of stock"/"Running low" pinned above, category/cadence/stock-state/staples-only filters + name-and-alias search, all server-rendered via a GET form and URL searchParams, no client filter state) + `/inventory/[id]` (purchase history, price trend, plain-language prediction explanation). `computeVirtualStockBase` (F9.2 implicit depletion, pure) shared by S-19's display and S-20's consumption actions. | lib/inventory/virtualStock.ts, lib/inventory/data.ts, lib/inventory/format.ts, app/inventory/page.tsx, app/inventory/item-card.tsx, app/inventory/sparkline.tsx, app/inventory/[id]/page.tsx, app/page.tsx (added an inventory nav link) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean; throwaway tsx fixture (deleted) -> computeVirtualStockBase: now==lastPurchasedAt returns raw exactly, N days elapsed subtracts dailyRate*N correctly, clamps to 0 past depletion, rateCorrection applied, null-rate fallback returns raw unchanged, negative-elapsed edge case (clock skew) doesn't go above raw; live-Supabase run against the real household (237 catalog items, 22 with positive raw stock, 0 with a rate signal or price history yet -- confirmed by direct query: no receipts have ever been committed to the real household, only onboarding `initial` movements, so every item correctly falls back to raw-stock display, exactly as designed) confirmed the fallback path; a second live run against the demo household found item_stats and price_history both empty there too (S-17's seeder writes stock_movements directly and never calls recomputeOneItem or commit_receipt, and S-18's validator is explicitly no-persistence per its own spec) -- so a real rate-signal path was exercised by inserting 3 real purchase movements + price_history rows for one real catalog item (Sugar), calling recomputeOneItem, confirming getInventoryItems/getInventoryItem produced sane values (virtualStockBase <= rawStockBase, dailyRateBase ~100/day matching the fixture's 1000g/10d spacing, avgUnitPrice90d=45 exactly, 3-point price series, 4 movements on the detail page including the pre-existing `initial` row, item correctly absent from getItemsNeedingAttention since stock was positive), then deleting every inserted row (stock_movements, price_history, item_stats, item_stats_history) to restore the real household to its pre-test state. | 8b15c11 | done | Design correction found during implementation (advisor unavailable, temporarily overloaded): predicted_depletion_at is anchored to the item's lastPurchasedAt (computeRateCrossCheck: `addDays(lastPurchasedAt, currentStockBase/dailyRate)`), not to the recompute's wall-clock `now` -- so virtualStock = rawStock - dailyRate*(now-lastPurchasedAt), depletion projected since the last purchase, not since the last recompute. Corrected sub-S-19.json's invariant text and the implementation before writing it, via first-principles derivation from the actual E-5 code, not asserted. "Predicted days remaining" reads predicted_next_purchase_at rather than predicted_depletion_at (a real scope decision, not in the original backlog text) since the latter is null whenever the rate-cross-check's q<0.70 gate isn't met -- using it would blank the field for a large share of real items. avg_unit_price_90d is computed at read time from price_history rather than reading the identically-named-but-never-written item_stats column (confirmed via grep before relying on it). "Grouped by category" uses the leaf category (matches E-3/E-4's existing category vocabulary), not the 16 top-level groups -- no story has asked for two-level grouping and the leaf name is what every other screen already shows. |
| Epic gate | Final advisor pass across the full changeset before shipping. | -- | Advisor caught one more gap: `categoryName`'s embed + coalesce-to-'Uncategorized' had never been checked against real data -- a broken embed would have silently grouped all 237 items under one heading with every earlier check still passing (stock/rate/price checks never touch categoryName). Verified live: 59 distinct real leaf category names returned, only 3/237 items fall to the 'Uncategorized' fallback (genuinely unmatched, not a broken join). Residual risk stated plainly rather than implied away: all verification in this epic is at the lib layer against live Supabase; no `force-dynamic` page (`/inventory`, `/inventory/[id]`, the Today addition) has actually been rendered, since `npm run build` does not execute dynamic routes and this repo has no dev-server-plus-signed-gate-cookie harness. Page-render checks are parked in `docs/MANUAL-TESTS.md`'s E-7 entries per this repo's existing convention for non-automated checks -- not performed as part of this epic's own verification. | -- | done | See residual risk above; also see PR body for the security statement on `recordConsumption` (the first client-callable write path in the app) and its household-scoping guarantee. |
| S-20 | `recordConsumption` (used_up / wasted / used_some fraction or amount) writing real stock_movements + triggering recompute (no rate_correction override); item detail action buttons + inline used-some choice; `getItemsNeedingAttention` fixed mid-implementation (see risks); Today stub's "Needs attention" section, conditional header copy. | lib/inventory/consume.ts, lib/inventory/data.ts (hasStockRow + gate fix), app/inventory/[id]/actions.ts, app/inventory/[id]/consume-actions.tsx, app/inventory/[id]/page.tsx, app/page.tsx | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean; live-Supabase run against the REAL modal case (Sugar: onboarding-only item, `hasStockRow=true`, `lastPurchasedAt=null`, no item_stats row) per the advisor's explicit instruction not to fabricate a purchase-history fixture -- `used_up` zeroed rawStockBase (1000->0), item appeared in `getItemsNeedingAttention()` only after the action (confirmed absent before), a second immediate `used_up` call wrote no additional movement (no-op guard), cleanup restored the item to its exact pre-test state; a second live run confirmed `used_some_fraction(0.5)` subtracts exactly 50% of the pre-action VIRTUAL stock (not raw) from raw stock, and `used_some_amount` exceeding raw stock clamps to exactly 0 rather than going negative; a third live run confirmed `reconcileRateCorrection`'s own consumption-signal gate query (lifted verbatim from lib/predictions/reconcile.ts) finds no row before a consumption action and finds one immediately after, on the same item -- closing S-15's documented dependency. Every inserted movement was deleted after each run. | 733ced9 | done | **Advisor caught a real bug before it shipped**, not a style note: the original `getItemsNeedingAttention()` filtered on `lastPurchasedAt !== null`, which excludes every item whose only history is an onboarding `initial` tick-off (no `item_stats` row exists until a PURCHASE event triggers `recomputeOneItem`) -- confirmed via live query that this is 22/22 of the real household's current positive-stock items, i.e. the modal case, not an edge case. Fixed by adding `hasStockRow` (presence of any `v_current_stock` row) as an OR'd discriminator; verified against a real onboarding-only item per the advisor's explicit instruction, not a fabricated fixture (which would have passed even with the bug present, exactly as the advisor warned). Also caught: `app/page.tsx`'s "Nothing due yet" copy was unconditional, which would have rendered directly above a populated Needs-attention list -- made conditional. Residual, stated rather than papered over: `recordConsumption`'s stock-available clamp is correct within a single request but does not close a genuine concurrent double-submit race (two in-flight requests reading the same pre-write stock); mitigated with a pending-state button disable in the UI, not eliminated -- an atomic DB-side check-and-write would be needed to fully close it, judged out of scope given this is a single-household app with no realistic concurrent-request scenario today. `getItemsNeedingAttention()` still won't include items with ZERO stock signal at all (no stock row, no purchase history) -- correct by design, that's "never touched this catalog item," not "needs attention." |
