| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-21 | `getPlanItems()` (F11's centerpiece): effective cadence bucket (`item_stats.cadence_override ?? cadence_bucket`, read for the first time since E-5 deliberately reserved the column), pack-size-aware suggested qty (guards `dailyRateBase` AND `intervalEstDays` null, not just the former), manual-action state (snooze/skip/exclude) persisted in the previously-unused `plan_entries` table with read-side auto-expiry (no regeneration cron -- see design deviation below). `getInventoryItems()` (E-7) now selects `cadence_override` and exposes the effective bucket everywhere (`cadenceBucket`), plus `computedCadenceBucket`/`cadenceOverride` split out, so `/inventory` and `/plan` never disagree once an item is overridden. `/plan`: one tab per cadence bucket (`?bucket=` searchParam, defaults to the first bucket with an active item), per-item suggested qty/due date/snooze/skip/exclude/move-bucket controls, "Revert to auto" when overridden. | supabase/migrations/20260730100000_plan_entries_unique_household_item.sql, lib/plan/data.ts, lib/plan/actions.ts, lib/inventory/data.ts (cadence_override select + effective-bucket read), lib/inventory/format.ts (CADENCE_BUCKET_ORDER, formatDueDate), app/plan/page.tsx, app/plan/plan-item-actions.tsx, app/plan/actions.ts | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, `/plan` registered as a dynamic route. Seeded the demo household for real multi-bucket data (`ENABLE_SEED=true npm run seed:history`, then a throwaway `recomputeAllItemsForHousehold(DEMO_HOUSEHOLD_ID)` run -- both real committed tooling, temporarily disabling `server-only` per this repo's convention, restored and confirmed via `git diff` immediately after) -- produced 67 items across 7 real buckets (fortnightly 21, unpredictable 11, weekly 14, monthly 11, quarterly 4, daily 3, every_2_3_days 3), closing a real gap the advisor caught before this: both households had zero `item_stats` rows going in, so the original test plan as written would have passed vacuously (`getPlanItems()` returning `[]` against an empty table). Live-Supabase throwaway script against the now-populated demo household verified: `getPlanItems()` count matches a direct `item_stats` count with non-null `cadence_bucket` (67=67); suggested-qty formula matches hand computation for a real rate+pack-size item (500 base units, pack-rounded up); cadence override round-trips (`setCadenceOverride` -> effective bucket changes -> `revert to auto` -> effective falls back to computed); **A9 verified directly**: called real `recomputeOneItem` against the overridden item afterward (simulating a nightly recompute) and confirmed `cadence_override` was untouched in the row; snooze/exclude/skip each write and read back correctly, including read-side auto-expiry for both snooze (past `snoozed_until`) and skip (due date manually advanced past the stamped anchor); excluded item confirmed absent from `getDueSoonItems()` even with a 9999-day window. Every row touched by the throwaway script was deleted afterward; `cadence_override` reset to null on the one item touched. Real page-render check (a gap every epic since E-7 had flagged and parked): ran `next dev`, minted a real signed gate cookie from `GATE_COOKIE_SECRET`, curled `/`, `/plan`, `/inventory` against the REAL (cold-start, zero-data) household -- all three returned 200, `/plan` rendered its tab bar with correct zero counts and "No items in this bucket yet.", no error boundary triggered. Dev server killed and throwaway cookie-minting script deleted afterward. | 7184101 | done | **Design deviation (advisor review before implementation):** F11's per-bucket running cost total ("~₹2,340") is deliberately not built. `price_history.unit_price` is priced per the receipt's original display unit (kg, piece, dozen, etc.) but that basis is never stored alongside the price -- only the raw number -- while `suggestedQtyBase` is in base units (grams/ml/pieces). There is no safe general conversion (a kg-priced item's per-gram cost is `unit_price/1000`; a piece-priced item's is `unit_price/1`; nothing in `price_history` distinguishes them). Computing a total anyway would silently produce numbers wrong by up to 3 orders of magnitude for weight/volume items. Scoped to F13 (Budget & insights), which needs to fix the underlying data gap first. **Design decision, recorded rather than left an accident:** a bucket tab lists every item assigned to that bucket (sorted by due date ascending), not only items literally due today -- F11's tabs are meant to be browsable groups ("here's everything you buy weekly"), and a due-today-only filter would leave most tabs empty almost always. Each row still shows its own due date. **plan_entries design:** rather than the item_stats/cadence_override "upsert excludes the column" idiom, plan_entries writes are explicit single-row upserts (added a unique index on `(household_id, catalog_item_id)`, additive migration on an empty table) because skip/snooze auto-expiry needs conditional logic, not blind column omission. |
| S-22 | `getDueSoonItems(daysThreshold=3)`: filters `getPlanItems()` to `planState==='pending'` items with `dueDate <= now+3d` (includes overdue, matching the existing `RUNNING_LOW_DAYS_THRESHOLD` convention rather than a new definition), sorted soonest-first. `app/page.tsx`: new "Due soon" card (forward-looking prediction) between the header and S-20's "Needs attention" (already-empty fact) -- distinct sections, an item can be in one/both/neither. Header copy now accounts for both counts so a cold-start household still shows the onboarding nudge. Added a "View plan" link (no global nav exists yet in this repo, same gap S-20 noted for `/inventory`). | app/page.tsx, lib/plan/data.ts (getDueSoonItems), lib/inventory/format.ts (formatDueDate) | Landed in the same commit as S-21 since both consume the same `lib/plan/data.ts` and were implemented together -- noted here rather than presented as two separate commits that didn't happen. Verified against the seeded demo household: `getDueSoonItems(3)` returned 11 items, every one's `dueDate <= now+3d` (including one ~2.5 months overdue, correctly surfaced), confirmed via the same throwaway script as S-21. Real page-render check (see S-21) confirmed `/` renders "Nothing due yet -- you're all set." and the "View plan" link for the real cold-start household without error. | 7184101 | done | Cold-start correctness (A13) relies on `getPlanItems()` returning `[]` when no `item_stats` row has a non-null `cadence_bucket` -- true by construction for a household with zero purchases, confirmed live against the real household above, not just asserted. |
| Epic gate | Final advisor pass across the full changeset before shipping. | -- | Advisor's pre-implementation review caught two real, otherwise-shippable gaps before any code was written: (1) the original test plan would have passed vacuously against two empty households -- fixed by seeding the demo household for real; (2) the skip-anchor date comparison (plan_entries.due_date is a `date` column, predicted_next_purchase_at is a `timestamptz`) would never have matched, silently making "skip once" a no-op -- fixed with `toKolkataDateString` on both sides plus a `>` (not `!==`) comparison so a re-estimate that pulls the date earlier doesn't un-skip a dismissal the user just made. Also flagged and confirmed clean: `commit_receipt()`'s `item_stats` upsert only sets `purchase_count`/`last_purchased_at`/`updated_at`, never `cadence_override` -- A9 holds against a real purchase commit, not just nightly recompute. A second advisor pass, run after the build was clean and both stories landed, caught three more gaps before shipping: **(a)** the A9 verification above had only exercised `recomputeOneItem` (single-row upsert), not `recomputeAllItemsForHousehold` -- the actual function the nightly cron route (`/api/cron/recompute-stats`) invokes, a batched array upsert with its own `toRow()` call sites. Re-verified live: set an override, ran the real `recomputeAllItemsForHousehold(DEMO_HOUSEHOLD_ID)` (67 items), confirmed `cadence_override` survived on the overridden item while `cadence_bucket` recomputed independently (fortnightly, unaffected by the `monthly` override) -- A9 now verified against the literal nightly path, not an adjacent one. **(b)** the earlier page-render check had only exercised the real household, which has zero `item_stats` rows -- so `/plan` rendered zero item rows, and the client-component boundary (`plan-item-actions.tsx`, a `'use client'` file importing seven server actions) and server-action reference serialization were never actually exercised. Fixed: set `onboarded_at` on the demo household (was null; harmless and arguably more correct for a fully-seeded household, left set rather than reverted), started `next dev` with `DEFAULT_HOUSEHOLD_ID` overridden to the demo household via shell env (confirmed process.env wins over `.env.local`, as Next.js documents), curled `/plan` and `/` with a real signed gate cookie against the now-populated household: both 200, response bodies (30-36KB, not a fallback/error page) contained real rendered controls ("Move to...", "Always exclude", "Skip once", "Due in 37 days") and correct live bucket counts (Weekly 14, Fortnightly 21, Unpredictable 11, matching the direct DB query), no error-boundary strings. **(c)** advisor flagged that "skip once" may have two different effective lifetimes depending on whether an item has cleared the rate-cross-check's `q>=0.70` gate: for gated items, `computeRateCrossCheck`'s `dailyRateBase = sumQty / daysBetween(windowEvents[0], now)` has a denominator that grows every recompute with no new purchase, so `dailyRateBase` shrinks, the depletion/next-purchase estimate extends, and a skip can auto-expire well before the item is actually due again; ungated items are fully anchored to `lastPurchasedAt` and their skip persists until a real purchase. Verified algebraically (pure `computeItemStats` call, same 3-event fixture, `now` advanced by exactly 1 day, no new events): `predictedNextPurchaseAt` drifted 6 hours later between the two calls -- confirming the mechanism is real, not hypothetical. Not fixed in this epic (the drift is inherent to E-5's already-shipped, already-verified rate-cross-check algorithm, not a bug in this epic's own code) -- stated here as a residual risk rather than left undiscovered. | -- | done | Residual risks stated plainly: (1) the estimated-cost feature is out of scope for this epic, see S-21's deviation note. (2) `plan_entries` single-row upserts are not protected against a genuine concurrent double-click on the same item producing two near-simultaneous writes to the same row (the unique index makes this an upsert-race, not a duplicate-row race, and the last write wins) -- acceptable at this app's real concurrency level (single household, no realistic simultaneous multi-device edits), same class of accepted risk S-20 documented for `recordConsumption`. (3) The estimated-cost gap (S-21) is a real, not-yet-closed piece of F11 as literally written; F13 is the natural epic to fix it once `price_history` carries a real per-base-unit price. (4) "Skip once" has an inconsistent effective duration across items depending on whether the item has cleared the rate-cross-check gate (confirmed real, see (c) above) -- a UX inconsistency inherited from E-5's algorithm, not this epic's own bug; worth a follow-up story if it proves confusing in practice (e.g. re-anchoring the skip to a fixed number of days rather than the next computed due date). |
