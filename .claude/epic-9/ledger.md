| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-23 | `generateShoppingList(source)`: builds candidates from `getPlanItems()`/`getDueSoonItems()` (E-8, reused unmodified), filtered to `planState==='pending'` for both source types, archives any prior active list before inserting the new one (generation replaces, never appends -- no unique constraint exists on `(shopping_list_id, catalog_item_id)`). `getActiveShoppingList()` reads the single active list joined with `catalog_items` display fields. `formatShoppingListAsText()` (pure) renders `[ ]`/`[x]` plain-text lines reusing `formatBaseQty`. `/shopping-list`: generate-by-bucket buttons + due-in-N-days form, item list with plain checkbox cross-off (no ledger write -- S-24 adds the price-logging path), "Copy as text" client button (`navigator.clipboard`, no toast library in this repo, 2s label swap for feedback). No migration -- `shopping_lists`/`shopping_list_items` already had every column this story needs since E-0's `full_schema` migration, confirmed by reading the schema before writing the spec, not assumed. | lib/shopping-list/generate.ts, lib/shopping-list/data.ts, lib/shopping-list/format.ts, app/shopping-list/page.tsx, app/shopping-list/actions.ts, app/shopping-list/copy-list-button.tsx, app/shopping-list/shopping-list-item-row.tsx | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, `/shopping-list` registered as a dynamic route. Live-Supabase throwaway script against the seeded demo household (E-8 left it with 67 items/7 buckets, not vacuous): generated from `bucket:'weekly'` -- item count matched a direct `getPlanItems()` filter exactly (14=14), one spot-checked item's `qty_base` matched its live `suggestedQtyBase`; generated again from `due_in_days:7` -- confirmed the first list flipped to `status='archived'` and exactly one `status='active'` row existed afterward (no duplicates); plain-text export header/format matched hand-built expectations; generating from a bucket with zero pending items created a list with zero `shopping_list_items` rows, not an error. Every row inserted was deleted afterward. Real page-render check (`next dev` + signed gate cookie + curl, matching E-8's recipe): the real cold-start household showed the empty state ("No active list") with no broken render; the demo household, after generating a real list via a throwaway script, showed all 14 weekly items rendered by name (`SYN 0xx` x14, confirmed unique), the list name "Weekly", "14 items.", and the "Copy as text" control -- both dev-server instances killed and the throwaway list cleaned up afterward. | 6174d3c | done | None outstanding. One design decision recorded rather than left an accident: both generation sources filter to `planState==='pending'` only (an item snoozed/skipped/excluded on `/plan` never reappears on a freshly generated list) -- deliberately narrower than `/plan`'s own bucket tabs, which show every item in a bucket regardless of state for browsability; a shopping list is meant to be purely actionable. |
| S-24 | `log_shopping_list_purchase(shopping_list_item_id, unit_price)`: new atomic Postgres function mirroring `commit_receipt()`'s (S-13) per-line bookkeeping -- one `stock_movements` purchase row (`source_receipt_id` null, load-bearing per `reconcile.ts`'s own existing comment), one `price_history` row, one `item_stats` upsert bump -- but race-safe via `FOR UPDATE` row lock + `purchase_logged_at IS NULL` guard inside one transaction (a genuine close of the double-checkoff race, not an accepted-risk note: a concurrent second call blocks on the lock and sees the guard already tripped once it acquires it). `lib/shopping-list/checkoff.ts`: `logShoppingListPurchase()` calls the RPC, then (only when the RPC reports it actually wrote) calls `recomputeOneItem()` with **no** `rateCorrectionOverride` -- S-15 reconciliation is receipt-commit-only by its own invariants, same reasoning `recordConsumption` (E-7) already established for consumption movements -- and resets any active `snoozed`/`skipped` `plan_entries` suppression for the item (not `excluded`, a deliberate stronger signal) via a new `resetSuppressionAfterPurchase()` in `lib/plan/actions.ts`. `setChecked()` is the no-price path: a plain `checked`-column update, never touches `purchase_logged_at`/`logged_price`/the ledger, in either direction (checking or unchecking) -- ADR-0001, history is never reversed by a list toggle. UI: `shopping-list-item-row.tsx` adds a per-row price input + "Log purchase" button, replaced by "Logged: ₹X" once `purchase_logged_at` is set. | supabase/migrations/20260730110000_shopping_list_purchase_logging.sql, supabase/migrations/20260730120000_log_shopping_list_purchase_ambiguous_column_fix.sql, lib/shopping-list/checkoff.ts, lib/shopping-list/data.ts (purchase_logged_at/logged_price added to the select + type), lib/plan/actions.ts (resetSuppressionAfterPurchase), app/shopping-list/actions.ts, app/shopping-list/page.tsx, app/shopping-list/shopping-list-item-row.tsx | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean. **Real bug caught by this story's own first live run** (not by review): the RPC's original `RETURNS TABLE(catalog_item_id uuid, household_id uuid, already_logged boolean)` failed immediately with "column reference \"catalog_item_id\" is ambiguous" -- plpgsql implicitly declares `RETURNS TABLE` columns as in-scope variables, colliding with the real `item_stats.catalog_item_id` referenced in `on conflict (catalog_item_id)`. Fixed via a second migration (`DROP FUNCTION` + recreate with `out_*`-prefixed columns, since `CREATE OR REPLACE` refused the OUT-parameter rename with "cannot change return type of existing function") -- same sequential-migration-fix convention S-13 used for `commit_receipt()` (`20260730073000`/`074000`). Live-Supabase verification against the real household (a fresh catalog item, zero prior history, `default_pack_size=280`): logged a purchase at price 42.5 -- `v_current_stock` +280 exactly, `item_stats.purchase_count` +1 exactly, `predicted_next_purchase_at` advanced (a real `computeItemStats` recompute ran), exactly 1 new `stock_movements` row (`type='purchase'`, `source_receipt_id=null`) and exactly 1 new `price_history` row, **`rate_correction` unchanged (1 -> 1)** -- direct proof reconciliation was skipped, not merely unexercised. Double-clicking the same row with a different price (999) afterward: zero additional `stock_movements`/`price_history` rows, `purchase_count` unchanged, `logged_price` still 42.5 -- the row-lock+guard held under a real repeated call. A separate fresh row: `setChecked(true)` then `setChecked(false)` -- zero ledger writes at any point, `purchase_logged_at`/`logged_price` stayed null throughout. **Cross-epic correctness check (the one way this epic could silently damage E-5, called out explicitly before implementation):** inserted a null-sourced purchase movement 2 days before a simulated receipt-commit instant, seeded plausible `item_stats` fields, then called the real `reconcileRateCorrection()` with a receipt id that does not match the null source -- `rate_correction` moved `1 -> 1.15` exactly as the UP_MULTIPLIER branch predicts, which is only reachable if the null-sourced movement was actually found as "the previous purchase" (the alternative, "no prior purchase found," short-circuits to an unchanged value for a different reason) -- confirms `reconcile.ts`'s `.or(source_receipt_id.neq.X, source_receipt_id.is.null)` null branch works for real, not just per its own comment. Every row/item_stats value touched by all three verification runs was reverted or deleted afterward. | 92a9526 | done | None outstanding. The RPC-signature bug above is the one real defect this epic produced, and it was caught and fixed by this story's own verification before merge, not left for later. |
