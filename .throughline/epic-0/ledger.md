# Epic E-0 — Foundation & platform scaffolding — ledger

| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-01 | Next.js 16 App Router scaffold, TanStack Query + Zod, env contract, Vercel git-linked deploy | app/, public/, package.json, tsconfig.json, eslint.config.mjs, next.config.ts, .env.example | `npm run build` -> pass; deployed URL -> 200 | a01e522 (merged to main via PR #9, e85187c) | done | Bumped Next.js 15->16 (ADR-0005 updated) since create-next-app's latest resolved to 16; merged to main ahead of the rest of the epic so Vercel's production branch had buildable code — S-02/S-03 still land via the epic branch as normal |
| S-02 | 14-table domain schema (households..ingest_jobs), RLS written-but-disabled, pg_trgm, v_current_stock; seed 75 categories (16 top-level + 59 leaf) + 237 catalog items | supabase/config.toml, supabase/migrations/20260728130915_full_schema.sql, supabase/migrations/20260728131120_rls_policies_disabled.sql, supabase/seed.sql | `supabase db push` -> pass; relrowsecurity=false on all 14 tables; fixture v_current_stock check (5 movement types, pre/post stock_epoch) -> 850 expected == 850 actual | 5906685 (schema+RLS), b3fa1a4 (seed+verify) | done | Placeholder household seeded at nil UUID standing in for DEFAULT_HOUSEHOLD_ID — S-04 onboarding must UPDATE this row in place, not INSERT a new one, or seeded catalog_items orphan. categories table has no household_id (global system-seeded taxonomy) — deliberate reading of ADR-0004, not literal "every table, no exceptions". 237 items vs the "~300" scope estimate — every one of the 59 leaf categories has real (non-filler) coverage; didn't pad to hit a round number |
| S-03 | Passcode gate: proxy.ts (Next 16's renamed middleware convention), signed HTTP-only cookie via Web Crypto HMAC-SHA256, /gate page + Server Action | lib/gate.ts, proxy.ts, app/gate/page.tsx, app/gate/actions.ts, .env.example | `next build` -> pass; manual: no cookie -> 401, tampered signature -> 401, real passcode -> cookie minted -> subsequent request 200; wrong passcode -> redirect+visible error | a82a1c1 | done | Used proxy.ts not middleware.ts — Next.js 16 deprecated/renamed the convention (found via the dev-server deprecation warning, confirmed in next's own bundled docs); function renamed middleware->proxy to match. Fail-closed: missing GATE_PASSCODE or GATE_COOKIE_SECRET env vars means every request 401s, never fail-open. GATE_PASSCODE/GATE_COOKIE_SECRET not yet set in Vercel — production deploy will 401 everything until that's done |
