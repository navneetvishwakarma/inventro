| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-04 | Onboarding wizard (household info -> 6 presets -> optional 46-item tick-off), Today landing page, first app-level Supabase client | app/onboarding/*, app/page.tsx, lib/supabase/server.ts, lib/household.ts, lib/onboarding/* | `npm run build` -> pass; full Playwright browser E2E (fresh household -> onboarding redirect -> all 3 steps -> Today) -> pass, 0 console errors; DB checks: 3 stock_movements type=initial, household name/budget/stock_epoch/onboarded_at all correct; no Supabase key in .next/static | 257560b | done | Found and fixed 2 real bugs via full E2E (not caught by build/curl alone): (1) tick-off list ballooned to 140 items because it re-fetched post-preset is_staple set instead of a fixed pre-preset snapshot — violated the "~40 items" spec; (2) both onboarding/Today pages were being statically prerendered at build time (no direct cookies()/headers() call, only an indirect Supabase fetch), which broke the Server Action's response entirely in production mode — fixed with `export const dynamic = 'force-dynamic'`. shadcn's `-d` default preset installed Base UI primitives, not Radix, contrary to what's documented as the flag default |
