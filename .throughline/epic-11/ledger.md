| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-29 | `search_catalog_items(p_household_id, p_query, p_limit)`: household-scoped, `is_archived=false`, empty query recency-ranked (`item_stats.last_purchased_at desc nulls last`), non-empty query ranked by `greatest(canonical_name similarity, best alias similarity, 0.5 ILIKE-contains floor)` filtered to score>0.15. `last_qty_base` per result via `coalesce(latest purchase qty_base, default_pack_size, 1)` -- same fallback chain `log_shopping_list_purchase` (S-24) already uses. `log_manual_purchase(...)`: existing-item path (validates household+not-archived) and new-item path (resolves category by slug exactly like `commit_receipt()`, inserts `catalog_items`+`item_aliases` source='manual') both converge on one `stock_movements`/`price_history`/`item_stats` write, `out_`-prefixed `RETURNS TABLE` per the S-24 fix precedent. `lib/catalog/search.ts` (`searchCatalogItems`), `lib/catalog/manual-entry.ts` (`logExistingItemPurchase`, `checkForDuplicate` -- reuses `matchCatalogItem` verbatim as a duplicate safety net before any new-item create, `createNewItemAndLogPurchase` -- reuses `normalizeUnitToBase` verbatim for qty conversion). `app/add/manual/`: search-then-confirm UI (debounced 250ms), prefilled qty, optional price, date field; new-item form (brand, category, qty+unit) gated behind the duplicate check. Linked from `/add` and `/`. **Follow-up migration (caught during this story's own live verification, before commit):** `uncategorized` is itself a resolvable leaf category (`parent_id` is not null, real `default_base_unit`), so the original `parent_id is not null` guard did not exclude it from new-item creation despite the intent stated in S-13's own spec comment for the receipt path -- `log_manual_purchase` now explicitly rejects `p_new_item_category_slug='uncategorized'`, and the client-side category picker filters it out of the list entirely. | supabase/migrations/20260730150000_manual_entry_search_and_purchase_rpc.sql, supabase/migrations/20260730151000_log_manual_purchase_uncategorized_guard.sql, lib/catalog/search.ts, lib/catalog/manual-entry.ts, app/add/manual/page.tsx, app/add/manual/manual-entry-form.tsx, app/add/manual/actions.ts, app/add/capture.tsx (Add manually link), app/page.tsx (Today card links) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass. **Live Supabase (real household, all fixture rows cleaned up afterward, row counts confirmed back at baseline 307/237/1157/0/67/141 across catalog_items/item_aliases/stock_movements/price_history/item_stats/item_stats_history):** empty-query recency ranking verified with a real two-item `last_purchased_at` fixture (newer ranked before older, both before null-`last_purchased_at` items); a deliberately reworded query ("choco" for "Chocos") ranked the real item first at score 0.625 via trigram; a 2-char query ("mi") returned ILIKE-floor matches at score 0.5. `log_manual_purchase` existing-item path: one `stock_movements` row, one `price_history` row, `item_stats.purchase_count`/`last_purchased_at` bumped correctly. New-item path: one `catalog_items` row with `base_unit` matching the chosen category's `default_base_unit`, one `item_aliases` row (`source='manual'`). Duplicate safety net exercised implicitly via `matchCatalogItem` reuse (same function S-09 already proved). Backdating passthrough: a manual purchase dated ~13.7 years in the past left `v_current_stock` unchanged (500 -> 500), confirming the view's own `occurred_at >= stock_epoch` filter handles it with no new code, positively confirmed rather than assumed. Uncategorized guard: `log_manual_purchase` with `p_new_item_category_slug='uncategorized'` raised the expected exception and created nothing. **Page-render smoke check** (`next dev` + signed gate cookie + curl, no headless-browser tooling available in this environment): `/add/manual` -> 200, category `<select>` confirmed to exclude `uncategorized`; `/add` -> 200 with the new "Add manually" link present. Client-side debounced search/select/confirm interaction itself was verified by code-trace against the already-proven RPC behavior, not driven by an automated browser -- documented gap, matching this repo's established precedent (S-26/S-27) for client-only interaction in an environment with no headless-browser tooling. | e964990 | done | Client-side search/select/confirm flow (debounce, result click, form state transitions) was not exercised by an automated interaction script -- verified by code-trace plus a real page-render smoke check only, per the documented environment limitation this repo has hit on every prior UI-heavy story. |
| S-30 | `merge_catalog_items(p_household_id, p_survivor_id, p_loser_id)`: locks both rows, rejects self-merge / archived-survivor / already-archived-loser, reassigns `item_aliases`/`stock_movements`/`price_history`/`receipt_lines.matched_item_id`/`shopping_list_items` (every UPDATE explicitly `household_id`-scoped, not just incidentally safe via the row locks), `plan_entries` with unique-constraint conflict handling (survivor's row wins, loser's dropped when both exist), archives the loser. **Advisor-caught before implementation:** `purchase_count` after merge -- `lib/predictions/recompute.ts`'s `toRow()` deliberately never writes it (owned by the commit paths), so deleting both items' `item_stats` rows and letting recompute re-upsert would silently drop or under-count it. Fixed by capturing both counts before any delete and writing `survivor_count + loser_count` explicitly into the survivor's `item_stats` row (insert-or-update) before the caller's `recomputeOneItem` runs, so the row exists with the right count regardless of whether the survivor had one before or whether there are any purchase events left to recompute over. `lib/catalog/manager.ts`: `getCatalogManagerItems()` (separate archived-inclusive query, deliberately not `getInventoryItems()`), `getMergePreview()`, `mergeCatalogItems()` (RPC + `recomputeOneItem` on the survivor), `archiveItem()`, `recategorizeItem()` (category_id only, base_unit immutable -- S-09's sticky-unit invariant -- rejects 'uncategorized'). `app/catalog/`: search/filter list (active+archived), per-item archive/recategorize, select-two merge flow with preview. Linked from `/inventory` (UX journey's stated trigger) and `/`. | supabase/migrations/20260730160000_catalog_manager_merge_rpc.sql, lib/catalog/manager.ts, app/catalog/page.tsx, app/catalog/catalog-manager.tsx, app/catalog/actions.ts, app/inventory/page.tsx (catalog manager link) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass. **Live Supabase, discriminating fixture (real household, two `ZZTEST`-prefixed items, each with 3 real `stock_movements` at different intervals -- survivor 10-day spacing, loser 5-day spacing, hand-derived expectation: combined 6-event EWMA must differ from the survivor-only 3-event EWMA, not just leave it unchanged):** raw RPC run confirmed aliases/movements reassigned (2 aliases, 3 movements), loser archived, loser `item_stats` rows gone, survivor `purchase_count`=6. Full TS wrapper (`mergeCatalogItems()`, `server-only` temporarily disabled per this repo's throwaway-verification convention and confirmed restored via `git diff --stat` before commit) re-ran the same fixture end to end: pre-merge survivor EWMA=10 (3 events, 10-day spacing) -> post-merge EWMA=5 (6 combined events, 5-day spacing) -- genuinely different, not vacuous; `purchase_count`=6 confirmed via the wrapper path too. Guards: self-merge, merge-into-archived-survivor, merge-of-already-archived-loser each raised the expected exception and wrote nothing. `plan_entries` conflict fixture (both survivor and loser pre-seeded with a row, which would violate the unique index if both were naively reassigned): confirmed exactly one row remains after merge, survivor's own `cadence_bucket` value preserved, no constraint violation. UX doc's stated success signal verified directly: a fresh `matchCatalogItem()` call against the loser's old alias text resolved to the survivor's id post-merge. `recategorizeItem()` verified moving an item into a category with a genuinely different `default_base_unit` (g-based category -> ml-based category): `category_id` changed, `base_unit` confirmed unchanged (not a vacuous check where old and new units happened to match); 'uncategorized' target rejected. `getCatalogManagerItems()` confirmed an archived fixture item remains visible (discriminates against accidentally reusing `getInventoryItems()`'s `is_archived=false` filter -- the specific gap advisor review flagged). All fixture rows (three separate runs) cleaned up in FK order; table row counts (catalog_items/item_aliases/stock_movements/price_history/item_stats/item_stats_history) confirmed back at baseline 307/237/1157/0/67/141 after each run. **Page-render smoke check:** `/catalog` -> 200 (real 307-item list renders, alias/movement counts present); `/inventory` -> 200 with the new catalog-manager link present. Client-side select-two/merge-panel/confirm interaction verified by code-trace against the already-proven RPC+wrapper behavior, not driven by an automated browser (same documented environment gap as S-29 and prior epics). | 4c29ad6 | done | Same client-interaction verification gap as S-29 (no headless-browser tooling in this environment) -- merge/archive/recategorize UI wiring verified by code-trace plus page-render smoke check, not a driven browser session. |
| Epic gate | Final `advisor` pass across the full changeset (both stories), run after both commits landed and `npm run build` was clean. Caught two real UI-layer defects and one structural risk in S-30, all fixed in this same pass before shipping: **(1)** `CatalogManager`'s `selectedItems` derivation didn't exclude an item that became archived after being selected for merge -- its checkbox disappears once archived (no path to deselect), so "Merge selected" kept offering a merge the RPC would always reject. Fixed by filtering `selectedItems` to `!isArchived`; the button now disappears instead, and a later new selection naturally evicts the stale id. **(2)** The recategorize `<select>` tracked a persisted `categorySlug` state that never reflected the item's actual category and, worse, kept displaying a FAILED recategorize's target as if it had succeeded across an unrelated page revalidation. Fixed by making it a stateless trigger (fixed `value=""`, no tracked selection) -- `item.categoryName` is now the only thing claiming to show the item's real category. **(3)** `getCatalogManagerItems()` fetched every household `stock_movements` row unbounded to compute a per-item `movementCount` -- the exact PostgREST 1000-row-cap truncation bug class `lib/inventory/data.ts`'s `price_history` query already guards against (S-18's live incident this repo hit before). Confirmed live via `{count:'exact', head:true}` vs. full-fetch `.length` comparison that this household (22 real movements) isn't truncated today, but it's a live footgun as purchase history grows with no bound in sight. Fixed by dropping `movementCount` from the list entirely -- `getMergePreview()` (bounded to exactly two ids via `.in()`) is the only place the acceptance criteria actually need it. | app/catalog/catalog-manager.tsx, lib/catalog/manager.ts | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, both `/add/manual` and `/catalog` routes registered. Re-verified live: `getCatalogManagerItems()` re-run against the real household (238 items, no `movementCount` field); `/catalog` page-render smoke check (`next dev` + signed gate cookie + curl) -> 200, movement-count text confirmed absent from the rendered page, recategorize control confirmed present. | c60c499 | done | None outstanding. |
