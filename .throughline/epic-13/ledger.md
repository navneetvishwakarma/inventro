| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-32 | F14/REQ-20: daily 07:00 IST digest (fires only when something is due within 3 days -- the literal acceptance clause), weekly Sunday 18:00 IST "next week's list ready" digest (7-day window, same mechanism), both via a real Resend integration, plus an in-app due-count badge. Reused S-22's `getDueSoonItems()` verbatim as the single "due soon" source for both digest windows and the badge rather than re-deriving a second definition. Additive `households.notify_email` (nullable text, no Settings write path yet -- same precedent as `monthly_budget`, S-33/E-14 will add the UI). Resend send is a plain `fetch` to `https://api.resend.com/emails`, no SDK dependency (its surface can't be exercised live either way given the key gap below) -- returns a structured `{sent, reason}` result, never a bare boolean or a fake success. Both cron routes (`/api/cron/digest-daily`, `/api/cron/digest-weekly`) reuse `/api/cron/recompute-stats`'s exact `CRON_SECRET` fail-closed auth pattern verbatim. Badge: extended the existing `/plan` bucket-count idiom ("Weekly (3)") to the Today page's "View plan" link, reusing that page's already-computed `dueSoon` list -- no new query. **Known, pre-declared gap (not a defect):** `RESEND_API_KEY` was confirmed blank in `.env.local` before any design work began, and `notify_email` has no write path in this story -- both were treated as a genuine, documented residual risk rather than a blocker (working spec F14 explicitly excludes web push from v1, so this is not the "no infra exists at all" case the epic's authority note was written for; the send code path is real and complete, just unexercised end-to-end live). Advisor consulted before any code was written on exactly this call; agreed: ship with an honest skip, verify the gate logic and skip-reason boundaries live, document the send-path gap explicitly rather than fabricate a credential or silently drop the story. | `supabase/migrations/20260731100000_households_notify_email.sql`, `lib/notifications/digest.ts`, `app/api/cron/digest-daily/route.ts`, `app/api/cron/digest-weekly/route.ts`, `lib/onboarding/data.ts` (added `notify_email` to `getHousehold()`'s select), `vercel.json` (two new cron entries), `app/page.tsx` (badge) | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; `npm run build` -> clean, both new routes registered as dynamic (`ƒ /api/cron/digest-daily`, `ƒ /api/cron/digest-weekly`). **Live Supabase, real household (`00000000...`), all fixture rows cleaned up afterward, `item_stats`/`plan_entries`/`households.notify_email` confirmed back at baseline (1/0/null) after every check:** migration applied cleanly (`npx supabase db push`, `migration list` timestamps matched), both real households show `notify_email=null` immediately after. Boundary re-verification of `getDueSoonItems()` at this epic's actual acceptance criterion, via a controlled 3-item fixture in the real household: an item due in 2 days (pending) appeared in both the 3-day and 7-day windows; an item due in 5 days (pending) appeared ONLY in the 7-day window, correctly excluded from the 3-day one; an item due in 1 day but `snoozed` (future `snoozed_until`) was excluded from BOTH windows despite qualifying on due date alone -- all three outcomes matched `buildDailyDigest`/`buildWeeklyDigest`'s `dueCount` exactly (1 and 2 respectively). Cron routes exercised live via `next dev` + curl with the real `CRON_SECRET`: missing/wrong `Authorization` -> 401 (both routes, same as `recompute-stats`); nothing due (post-cleanup, genuine live state) -> `{sent:false, reason:"nothing due within 3 days"/"nothing due within 7 days", dueCount:0}`, confirmed no Resend call attempted; something due + `notify_email` still null (real state) -> `{sent:false, reason:"no notify_email configured"}`; something due + `notify_email` temporarily set + `RESEND_API_KEY` still genuinely unset -> `{sent:false, reason:"RESEND_API_KEY not configured"}`; something due + `notify_email` set + `RESEND_API_KEY` temporarily set to a dummy value + `RESEND_FROM_EMAIL` genuinely unset -> `{sent:false, reason:"RESEND_FROM_EMAIL not configured"}` (this fourth check was added after advisor caught that the original code hardcoded `from: 'Inventro <notifications@inventro.app>'`, a domain this project doesn't own -- Resend rejects unverified sending domains, so that was a fabricated default masquerading as a working one; fixed to read `RESEND_FROM_EMAIL` from env with its own honest skip, `.env.example`/`.env.local` updated, `.env.local`'s temporary dummy `RESEND_API_KEY` reverted to blank immediately after this one check) -- all four skip reasons confirmed distinct, never a fabricated `sent:true`. In-app badge page-render check (`next dev` + signed gate cookie + curl `/`): "View plan" renders with no badge when `dueSoon.length===0` (the real household's actual current state) and renders "View plan (1)" when a temporary due-soon fixture was live, matching the "Due soon" card's own count exactly. One incidental fixture-hygiene finding during this verification, not a code defect: an early manual test accidentally created a `plan_entries` row referencing a catalog item from the OTHER (demo) household while writing it under the real household's `household_id` -- caught and deleted before any assertion relied on it; row counts confirmed unaffected. | e395724, fda30d6 | done | (1) Three activation prerequisites remain before a real digest email is ever actually sent: a real `RESEND_API_KEY`, a `RESEND_FROM_EMAIL` on a domain verified in the Resend account, and a `notify_email` write path (E-14/S-33, `households.notify_email` has none yet). None are fabricated or stubbed to look ready -- all three currently produce their own honest, distinct skip reason, verified live. Close this out when all three land. (2) Vercel Hobby-plan cron timing is only guaranteed within the specified hour, not to the exact minute (confirmed via Vercel's own docs before adding the cron entries) -- the daily digest may fire anytime in the 07:00-07:59 IST window rather than exactly 07:00, same class of imprecision the existing nightly recompute-stats cron already accepted. (3) No global nav exists in this app (every prior UI-adding epic has noted this) -- the badge lives on the Today page's "View plan" link, the one surface every session lands on, rather than a persistent nav-level indicator. |

| Incidental fix (not E-13 scope) | `lib/receipts/canonicalize.ts`'s `normalizeUnitToBase` multi-pack `"N x M<unit>"` branch (e.g. `"2 x 500ml"`) computed `qty_base` from the pack encoding alone, ignoring how many such packs were actually purchased -- buying 2 units of a `"2 x 500ml"` item undercounted `qty_base` as 1000 instead of 2000. Flagged but not fixed while writing E-12/S-31's `commit_receipt` price fallback (see `.claude/epic-12/ledger.md`'s S-31 risks). Fix makes this branch consistent with the already-correct `pack of N` branch's long-standing "qty = packs purchased" assumption (`n * (qty ?? 1)`) -- introduces no new risk class, just closes the one branch that had drifted from it. Confirmed via a before/after `npx tsx` fixture script (deleted after, per repo convention; `server-only` temporarily commented out in `canonicalize.ts` and `lib/supabase/server.ts` for the run, both confirmed restored via `git diff` before committing): `qty=1` unaffected at 1000 both before and after; `qty=2` corrected from 1000 (bug) to 2000 (correct); `qty=null` still defaults to a multiplier of 1; `"pack of 6"`/`qty=2` -> 12 unaffected by the fix, confirming that branch was never buggy. **Left deliberately untouched:** E-12's `commit_receipt` SQL fallback (`price_history.price_per_base_unit`'s secondary derivation) still skips any line with a non-null `pack_size` specifically because of this bug -- that skip was correct defensive behavior against the old code and remains correct/safe now; widening it to trust `pack_size` lines again is a separate decision for whoever revisits that fallback, not implied or done by this fix. | `lib/receipts/canonicalize.ts` | `npx tsc --noEmit` -> pass; `npx eslint app/ lib/` -> pass; throwaway `npx tsx` fixture (4 cases above), deleted after, never committed. | 23fa17b | done | None -- single-line arithmetic fix, pure function, no schema/API surface change, fixture-verified before and after. |
