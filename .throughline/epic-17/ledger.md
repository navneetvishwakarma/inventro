| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-42 | vitest + Playwright runners wired to package.json | `vitest.config.mts`, `playwright.config.ts`, `package.json`, `.gitignore` | `npm test` / `npm run test:e2e` proven red-then-green with temporary smoke assertions (removed before commit) | 5e727ae | done | Branch-mixup caught and fixed mid-story: `implement-epic`'s branch-creation step (`git checkout main` -> new branch) left the S-42 commit sitting on a branch without E-17's own backlog/spec commit underneath it. Cherry-picked 1deae38 onto epic/E-17-test-infrastructure (now 3c5c547) before continuing, verified zero diff, deleted the stale branch. No risk to shipped code — caught by validate.mjs's epic/story count before any further story was built on the broken base. |
| S-43 | computeItemStats fixture tests (S-14a/c) | `lib/predictions/computeItemStats.test.ts` | `npm test -- computeItemStats` — 5 passed; validity spot-checked via a temporary `OUTLIER_REJECTION_MIN_N` mutation (confirmed the outlier test fails for the right reason, then reverted with zero diff) | 935fc15 | done | S-14b (rate-blended next-purchase date, perishability clamp) intentionally out of scope — MANUAL-TESTS.md's recorded fixtures don't cover it with concrete expected values, and inventing untested-in-production numbers would be worse than not testing it. Logged as a real coverage gap, not silently dropped. |
| S-44 | normalizeText + normalizeUnitToBase unit tests | `lib/receipts/canonicalize.test.ts`, `test/stubs/server-only.ts` (new), `vitest.config.mts` | `npm test -- canonicalize` — 12 passed; full `npm test` — 17 passed | da1e930 | done | Discovered `canonicalize.ts`'s `import 'server-only'` throws unconditionally under plain Node (vitest has no client/server bundler split for that guard to key off) — fixed via a `resolve.alias` stub in vitest.config.mts rather than per-file mocking, so S-45's `reconcile.ts` (same guard) doesn't hit the same wall. |
| S-45 | reconcileRateCorrection unit tests, mocked Supabase client | `lib/predictions/reconcile.ts` (constants exported), `lib/predictions/reconcile.test.ts` | `npm test -- reconcile` — 9 passed; full `npm test` — 26 passed; validity spot-checked via a temporary `DOWN_MULTIPLIER` mutation | 604dd31 | done | Two fixture bugs caught by the tests themselves during writing (previousPurchaseAt accidentally equal to thisPurchase.occurredAt tripped the early-return guard) — fixed before commit, left as evidence the mock chain is exercising the real early-return logic, not just the happy path. |
| S-46 | E2E route smoke suite (amended: route smoke only) | `e2e/fixtures.ts`, `e2e/gate.spec.ts`, `e2e/routes.spec.ts`, `playwright.config.ts` | `npm run test:e2e` — 14 passed, 1 honest skip (bucket-tab test skips when the seeded household has <2 populated buckets); stable across 3 consecutive runs | 715bc8a | done | **Amended mid-story** (Step 3 of implement-epic): backdating-vs-stock_epoch was descoped after discovering it needs a real LLM call (non-deterministic, costs money per run) via the receipt-capture flow, or a seeded-DB harness neither this story nor S-44 built. Deferred to new S-47 (define-backlog reconcile, GH #95 linked to epic #89). Also found and fixed three real test-authoring bugs, not app bugs: (1) `.first()`/`.last()` text-identity matches collided with hidden Sidebar/TabBar/MobileTopBar duplicates depending on viewport -- resolved by testing at mobile viewport (this app's primary real usage) and picking whichever end of the match list is guaranteed visible per element; (2) an unscoped role/name regex over the whole page matched unrelated elements -- resolved by scoping to the Tabs component's own `role="group"`; (3) `Element.textContent` and the ARIA accessible-name algorithm normalize whitespace differently, so re-querying a clicked tab by captured text after a searchParams-driven navigation failed -- resolved via `page.waitForURL` + re-querying the same lazy locator instead of matching by text. Separately observed (not fixed, logged for REQ-29's Inventory page-brief): the last item in a long Inventory list can be obscured by the fixed bottom TabBar at mobile viewport if the page doesn't reserve bottom padding for it. |
| S-47 | seeded-DB integration harness: alias matching (pg_trgm) + commit_receipt backdating vs. stock_epoch | `test/integration/seed-helper.ts` (new: seed/cleanup helper, writes only to `DEMO_HOUSEHOLD_ID` via `assertDemoHousehold`), `test/integration/seeded-db.test.ts` (new, 5 tests), `vitest.integration.config.mts` (new, separate config so this stays out of the fast/offline `npm test` suite), `package.json` (`test:integration` script) | `npm run test:integration` — 5/5 passed (2 consecutive runs); `npx tsc --noEmit` clean; `npx eslint` clean; `npm run build` clean; full `npx vitest run` unaffected (31/31) | dc7a3f9 | done | Status marked `in_progress` in `backlog.json` since a much earlier reconcile but never actually built (confirmed via `git log`/`gh issue view 95` — no branch, no commits, issue still OPEN); treated as not-started. Demo household (`DEMO_HOUSEHOLD_ID`, hardcoded, distinct from `.env.local`'s `DEFAULT_HOUSEHOLD_ID`) had zero `item_aliases` rows despite 70 seeded `catalog_items` — S-09's alias-backfill migration only ran once against whatever existed at migration-apply time, so items seeded later (via `seed-history.ts`) never got aliases. This meant no real alias data to corrupt or collide with; fixture aliases are the only ones present for any item this suite touches. Confidence-band test values (fuzzy-match landing in 0.40-0.62, miss below 0.40) are real calibrated pg_trgm `similarity()` scores against a real seeded alias, not guessed or hardcoded to the exact float (asserted as a range) — see script output in this session's transcript: exact=1.0, "amul taaza toned milk 500 ml"=0.862, "amul taza toned milk"=0.679, "amul toned milk"=0.593, unrelated text=0.016. Verified the acceptance's own explicit constraint directly rather than assuming it: queried `catalog_items` for the demo household before and after two consecutive full test runs (70 both times, zero leftover fixture rows matching the `ZZ-S47-FIXTURE` prefix), then ran `npm run validate:predictions` and confirmed it still PASSes at the same 80% (60 items scored, identical to a pre-harness baseline run) -- the harness cannot corrupt the demo household's row counts. No FK cascade exists in this schema (`supabase/migrations/20260728130915_full_schema.sql`), so cleanup deletes in explicit dependency order (stock_movements/price_history/item_stats/item_aliases/receipt_lines before catalog_items/receipts) rather than relying on `ON DELETE CASCADE`. `seed-helper.ts` is a new service-role writer (bypasses RLS entirely, same as any script under `scripts/lib/`) -- its safety rests entirely on reusing `assertDemoHousehold` unchanged (re-reads `is_demo` fresh every call, refuses if `DEMO_HOUSEHOLD_ID === DEFAULT_HOUSEHOLD_ID`) rather than writing a new guard, which is why this didn't need a separate security-agent pass: no new authorization surface, just a new caller of an already-audited one. |
| S-54 | pdf-parse DOMMatrix ReferenceError breaks the native-PDF-text fast path | `lib/llm/pdf-text.ts` (CanvasFactory passed explicitly to PDFParse), `next.config.ts` (`@napi-rs/canvas` added to `serverExternalPackages`), `lib/llm/pdf-text.test.ts` (new) | `npx vitest run lib/llm/pdf-text.test.ts` — 5/5 passed; full `npx vitest run` — 31/31; `npx tsc --noEmit` clean; `npx eslint` clean; `npm run build` clean | 140985d | done | Applied pdf-parse's own documented Next.js/Vercel troubleshooting fix for this exact error string (fetched from the maintainer's `docs/troubleshooting.md`, not guessed). **Could not reproduce the production failure locally** despite trying three fidelity levels against a hand-built native-text PDF: (1) a bare Node script importing `pdf-parse` directly, (2) `next dev` (Turbopack) via a temporary ungated debug route, (3) `next build && next start` (production bundling) via the same route — all three succeeded cleanly both before and after the fix, with `DOMMatrix` confirmed `undefined` globally throughout. This points to the failure being specific to Vercel's serverless *deployment* packaging (distinct from any locally-reproducible Next.js execution mode), consistent with `@napi-rs/canvas`'s native addon needing to be excluded from bundling rather than a plain missing-polyfill issue -- `@napi-rs/canvas-linux-x64-gnu` is already present in package-lock.json, ruling out a missing-platform-binary theory. The debug route was temporary (created under the ungated `/api/cron/` proxy-matcher path to test without auth, deleted before commit) and does not ship. Confirmed the bug is real via `mcp__claude_ai_Vercel__get_runtime_errors` (not just the story's own prior note): the exact error string occurred 3 times on `/add` in production, 2026-07-31T10:02:23Z-17:57:10Z, immediately before this fix. Real confirmation the FIX works requires observing that same tool for the absence of new occurrences after a real PDF upload against the post-merge deployment -- flagged to the user as a residual verification step, not silently claimed as fixed-and-proven pre-merge. The committed `pdf-text.test.ts` is general regression coverage for `extractPdfText`/`looksLikeRealDocument` against a real `PDFParse` call (not a mock) but, per the above, cannot itself detect a regression that drops the `CanvasFactory` argument, since that exact call succeeded in every local repro attempt with or without the fix -- the test comment was corrected to say this plainly rather than overclaim, the same overclaim class caught in S-72's perishability assertion. |
