| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-82 | logout control bound to existing logoutAction | app/(app)/settings/page.tsx (new Session card, form action={logoutAction}), e2e/auth.spec.ts (new logout test) | e2e auth.spec.ts 6/6 (2 consecutive runs, one against a paused-then-restored Supabase project -- see risks) | 41a0766 (fix), 6848206 (feat) | done | Hit a genuine remote-environment outage mid-story: NEXT_PUBLIC_SUPABASE_URL's project was paused (NXDOMAIN), blocking e2e for this and every subsequent story since most of E-25 verifies via e2e against a real session. Stopped and flagged rather than proceeding on unverified code; human unpaused the project via the Supabase dashboard. Once restored, first real run caught a genuine test bug (not an environment artifact): the new logout test navigated straight to /settings after signup, but Settings redirects to /onboarding until household.onboarded_at is set -- the "Log out" button was never reachable. Fixed by walking the 3-step onboarding wizard first, matching the pattern already established in e2e/fixtures.ts's shared authenticated fixture. Deliberately did not import that shared fixture into auth.spec.ts -- this file's own header comment documents that it intentionally uses the raw `base` test, not the shared fixture, because it specifically tests the auth/onboarding boundary rather than what happens once inside it. |
| S-83 | inline error handling + retry-safety for the onboarding wizard | lib/onboarding/data.ts (idempotency guard on stock_movements insert), lib/onboarding/data.test.ts (new), app/onboarding/actions.ts (result-type instead of throw, redirect kept outside try/catch), app/onboarding/wizard.tsx (inline Alert, state preserved on failure), e2e/onboarding.spec.ts (new) | unit lib/onboarding/data.test.ts 3/3, e2e onboarding.spec.ts 2/2 (independently re-run against live Supabase, not just sub-agent self-report) | 440718a, c739a06, 9db9a22 | done | Idempotency guard is scoped per catalog_item_id + household + type='initial', not household-wide -- a receipt-commit RPC also writes type='initial' movements for unrelated items, and a household-wide guard would have wrongly skipped legitimate onboarding movements for a household that already has any unrelated initial movement. Verified the redirect('/') call sits outside the try/catch (required -- Next.js implements redirect as an internal throw that a blanket catch would swallow). Found and fixed a real e2e flakiness source in its own new test: a bare page.getByRole('alert') collided with Next.js's built-in route announcer, which also carries role="alert" and is always present in the DOM -- fixed by asserting on the wizard's actual error text. Independently re-verified: re-ran unit + e2e tests myself (not trusted from the sub-agent report alone) and read the actions.ts/data.ts/wizard.tsx diffs directly to confirm the control-flow claims. |
| S-84 | per-file preprocessing failure handling in capture.tsx, without hanging the upload UI | app/(app)/add/capture.tsx (per-file try/catch, finally-wrapped status reset, grouped-mode partial-failure guard), e2e/capture-preprocess-failure.spec.ts (new) | e2e capture-preprocess-failure.spec.ts 1/1 (independently re-run against live Supabase + real LLM extraction call) | 0295aea | done | Scope legitimately expanded beyond the literal story text: grouped mode (2-3 screenshots as one order, S-25) needed its own guard -- if one image in a group fails preprocessing while others succeed, naively uploading the survivors would silently create a receipt from a partial group, violating S-25's "one indivisible unit" invariant enforced server-side. Fixed by skipping the network call entirely and failing every file in the group when any file in it fails preprocessing, with a distinct groupError message. `setStatus('idle')` now sits in a `finally` around the whole try block, so it resets on any exception path, not just the specific preprocessing-failure case -- broader defense-in-depth than the letter of the acceptance criterion asked for. No dedicated component-test layer exists in this repo (no jsdom/RTL, zero .test.tsx files) -- verification is e2e-primary against the live dev server and Supabase, consistent with how this app's client-component behavior is verified elsewhere. Independently re-verified: re-ran the e2e test myself and read the capture.tsx diff directly to confirm the grouped-mode logic and the finally-wrapped status reset. |
| S-85 | zero-line extraction now fails the quality check and escalates/fails instead of silently landing as `status:'parsed'` | lib/llm/extract.ts (checkExtractionQuality exported, zero-lines branch added), lib/llm/extract.test.ts (new, 5 tests), lib/llm/run-extraction-escalation.test.ts (new -- mocks Supabase client + Gemini provider at the boundary, runs the real runExtraction state machine in-process) | unit 6/6 across both new files (independently re-run, red/green both verified by the sub-agent by stashing the fix), e2e review-states.spec.ts 4/4 (see risks -- required 5 runs to get a clean pass) | 882f710 | done | Investigated whether zero lines is ever legitimately correct before hard-failing on it (per this story's own invariant): confirmed no -- schema.ts has no minimum-length constraint, but the prompt marks delivery/tax/discount lines as is_non_inventory:true rather than omitting them, so a real document always produces >=1 line; zero lines only means the model returned nothing usable. No code change was needed for "review queue badges it distinctly" -- S-67's existing statusBadge()/FailedCard logic already covers any non-'parsed' status by construction; confirmed by trace, not assumed. Independently re-ran e2e/review-states.spec.ts 5 times: got 4/4, 3/4, 2/4, 3/4, then 4/4 clean -- three different tests failed at different times (line 63, 157, and one unlabeled), never the same one twice in a row, and every failing test passed cleanly when run standalone. Each test in this file is fully self-contained (own signup, own household, own service-role-seeded fixtures, own cleanup) with zero shared state between tests, and the specific test S-85 touches (the zero-line-adjacent one at line 63) passed 3 of 4 runs -- this matches the Chromium/Turbopack resource-pressure flakiness already documented in this repo's own E-20/E-21/E-22 ledger entries, predating this story, not a regression introduced here. Recording rather than hiding: this file's flake rate under sequential full-file execution in this sandbox is real and un-fixed by this story (out of scope -- an infra/CI concern, not a code defect). |
| S-86 | cost/token accumulation across escalation now sums both attempts, not just the winner | lib/llm/extract.ts (Attempt's ok:false branch now carries usage; toUsage() normalizes NoObjectGeneratedError's raw LanguageModelUsage; addUsage() helper unifies both accumulation call sites, capturing the primary attempt's usage before `attempt` is reassigned on escalation), lib/llm/run-extraction-escalation.test.ts (3 new tests, extended the shared Supabase mock with a receipt_lines table handler) | unit lib/llm 14/14 (independently re-run) | c65a2af | done | Confirmed via a live node -e check against node_modules/ai that NoObjectGeneratedError genuinely carries a `usage: LanguageModelUsage | undefined` field before relying on it -- both of attemptExtraction's failure paths (quality-check failure, schema-validation failure) can now carry real billed usage, not just the success path. Verified with hand-computed expected cost numbers (estimateCostUsd per tier, summed) rather than a loose "greater than before" assertion. Flagged, correctly out of scope: a fully-failed extraction (both attempts fail) now computes a correct combined cost internally but that value is never persisted anywhere -- the failure-branch ingest_jobs.update doesn't carry cost/tokens. Pre-existing gap, not introduced by this story, noted for a future story rather than silently expanded into. Independently re-verified: re-ran lib/llm's full unit suite myself and read the extract.ts diff directly to confirm the addUsage/toUsage logic and the tier-per-attempt correctness. |
| S-87 | Catalog's Recategorize select gets an accessible name | app/(app)/catalog/catalog-manager.tsx (aria-label="Recategorize item"), e2e/catalog-a11y.spec.ts (new) | e2e catalog-a11y.spec.ts 1/1 | 7b5bbbc | done | Small enough (one prop) to implement directly rather than dispatch a sub-agent -- still done test-first: wrote the e2e test using an accessible-name-based locator (getByRole('combobox', { name: /recategorize item/i })) first, confirmed it fails against unmodified code (5s timeout, control not found by accessible name), added the aria-label, confirmed green. No axe-core or other a11y-testing infra exists in this repo; Playwright's own accessible-name-based role locators serve as a legitimate, zero-new-dependency proof that the fix works, since they only match an element that genuinely has the claimed accessible name. |
| S-88 | 2 of 3 bundled fixes shipped; 3rd stopped-and-flagged as a deeper architectural gap, not implemented | app/(auth)/signup/actions.ts (weak_password disambiguated from the generic anti-enumeration message; SignupResult gets a `field` discriminator), app/(auth)/signup/page.tsx (password-mismatch routed to confirmPassword Input's error prop instead of a page Alert), e2e/auth.spec.ts (2 new tests) | e2e auth.spec.ts 8/8 (independently re-run) | b3c2920 (Fix 1), 21c0d9a + fdd90f4 (Fix 2) | done (partial scope -- see risks) | **Fix 1 (field-level mismatch error) and Fix 2 (weak-password message) done and verified.** Fix 2 is security-sensitive -- signup/actions.ts deliberately collapses every failure into one generic anti-enumeration message (S-58's design, extended to signup). Verified independently (not just trusted from the sub-agent report): confirmed `weak_password`, `email_exists`, and `user_already_exists` are distinct, officially-documented error codes in the installed @supabase/auth-js SDK's own error-codes.d.ts, corroborating the sub-agent's claim (verified against GoTrue source directly) that password-strength validation is unconditional and independent of email-existence state -- disambiguating `weak_password` specifically cannot reopen the enumeration oracle. Every other/unknown code still falls through to the generic message (allowlist, not denylist). Re-ran the full auth.spec.ts myself: 8/8 clean, including the duplicate-email regression test asserting the exact generic text is byte-for-byte unchanged. **Fix 3 (tick-off staleness) NOT implemented -- stopped and flagged, not silently dropped.** Investigation found the story's own premise was incomplete: `create_household_for_user` (the signup RPC) creates zero `catalog_items`, and since every route redirects to `/onboarding` until complete, nothing seeds catalog items before then -- a fresh signup's tick-off list is always empty today, not just occasionally stale. This is a real, more severe regression than the story described (REQ-01's preset/tick-off feature is non-functional for real users), traced to the ADR-0004->ADR-0006 multi-tenant migration, predating this epic. A correct fix needs new migration/schema work (catalog seeding at signup + a curated-baseline marker distinct from is_staple, since applyPresets must keep bulk-mutating is_staple for cold-start priors while the tick-off list stays bounded) -- out of this story's stated in_scope (page.tsx + data.ts only). Flagged to the user for a scope decision (new story vs. deferred) rather than forced into an incomplete fix. One coverage-run anomaly noted, not chased further: `node scripts/coverage.mjs --story S-88` failed once on an unrelated, fully-mocked S-86 unit test (`extractStructured` called 3 times instead of 2) under `vitest --coverage`; did not reproduce on 2 immediate retries of the identical command, nor under plain `npm test`, nor under `vitest run --coverage` standalone -- consistent with transient sandbox resource contention from this session's high volume of concurrent test-runner invocations, not a code defect. |
| S-89 | enabled Button and Checkbox show cursor-pointer, matching Radio/ListRow's existing convention | components/ui/button.tsx, components/ui/checkbox.tsx (both className-only, cursor-pointer added to the enabled state), e2e/cursor-pointer.spec.ts (new) | e2e cursor-pointer.spec.ts 2/2 | 4b24074 | done | Implemented directly (single className addition to each of two files) rather than dispatching a sub-agent, still test-first: wrote an e2e test asserting `getComputedStyle(el).cursor === 'pointer'` on a real enabled Button (login submit) and a real enabled Checkbox (onboarding step 2 preset), confirmed both fail against unmodified code (`'default'`), added `cursor-pointer` to each component's base class string, confirmed green. A concurrent `tsc --noEmit` run surfaced parse errors in `.next/dev/types/routes.d.ts`/`validator.ts` -- confirmed these are gitignored, Next.js dev-mode auto-generated route-type files, being actively rewritten by one of several orphaned dev-server processes left running from this session's earlier test invocations (10+ stray node.exe processes observed); unrelated to this change (a className-only edit cannot produce a TS parse error in generated route files) and not chased further. |
| S-91 | six per-line review action buttons (Save & match, Confirm as new item, Actually match existing, Mark non-inventory, Retry extraction, Confirm/Update date) raised from 32px to a 44px min-height touch target | app/(app)/review/[id]/review-detail.tsx (className="min-h-11" added to each named Button, size="sm" kept so padding/text stay compact), e2e/review-touch-targets.spec.ts (new, 2 tests: NeedsReviewRow's three buttons + Update date via getBoundingClientRect, and FailedCard's Retry extraction) | e2e review-touch-targets.spec.ts 2/2 (independently re-run twice consecutively, clean both times, including once combined with review-unsaved-edits.spec.ts and catalog-a11y.spec.ts with no interaction regressions) | bbe3583 | done | **Real, pre-existing, unrelated bug surfaced while running the full review-states.spec.ts file for regression-checking (not introduced by this story or S-90):** all 4 tests in that file now fail deterministically -- `getReceiptForReview` returns null (real 404, confirmed in dev-server logs, not a flaky assertion) for every service-role-seeded receipt, immediately after the browser completes onboarding. Reproduced 3x standalone and 3x as a full-file run, both with my S-90/S-91 changes present and reverted to the pre-S-90 baseline (commit 362f18b) -- fails identically on both, proving neither story caused it. Wrote a standalone Node script (not committed, deleted after use) replicating the exact signup -> create_household_for_user RPC -> service-role seed -> RLS-scoped readback chain outside Playwright/the browser entirely: every step succeeded, household_id matched, and the session-scoped read (mirroring getReceiptForReview's own query) returned the row cleanly. This proves the DB/RLS/fixture-seeding pattern itself is sound -- the bug is specific to the real browser-driven flow in this one file, not the data layer, and not shared by review-touch-targets.spec.ts or review-unsaved-edits.spec.ts's nearly-identical signUpAndOnboard + service-role-seed pattern, both of which pass reliably in this same session. Not chased to a root cause given time already spent and that it doesn't block S-91 (which has its own clean, dedicated, independently-verified e2e coverage) -- but this is NOT the same "different test fails each run under resource pressure" flakiness recorded for S-85; it is the same 4 tests failing the same way every time, and must be root-caused before the epic quality gate (Step 4) or ship-epic, since a 100%-failing core review-state test file will fail that gate's full-suite run. Flagging explicitly rather than letting it surface as a surprise later. Also recovered from a self-inflicted near-loss during S-90 (see that row) that briefly touched this story's timeline; unrelated to the review-states.spec.ts finding above. |
| S-96 | Settings now shows a visible warning Alert (not just small gray helper text) when digest email delivery is unconfigured, reflecting live env state | app/(app)/settings/page.tsx (passes `digestConfigured={Boolean(process.env.RESEND_API_KEY)}` to SettingsForm, computed fresh per-request in this force-dynamic Server Component), app/(app)/settings/settings-form.tsx (new `digestConfigured` prop, Alert tone="warning" rendered when false, helper text trimmed of the now-redundant "once email delivery is configured" clause), e2e/digest-config-warning.spec.ts (new) | e2e digest-config-warning.spec.ts 1/1 (independently re-run twice consecutively, clean both times, including once against a freshly restarted dev server to prove the live-read invariant) | dadf00c | done | Discovered mid-story, not assumed: this repo's own .env.local declares `RESEND_API_KEY=` with an empty value, not an absent key -- `Boolean('')` is false, matching digest.ts's own `if (!apiKey)` gate, so this dev environment's digest is genuinely, correctly unconfigured today. First test draft wrongly assumed the key was live/set (based on `grep -c` finding the key name present) and asserted the warning should be absent; failed against real running app twice, including after a full dev-server restart specifically to rule out a stale-process env-caching explanation -- that restart also positively proved the "must reflect live config, not a static message" invariant, since it's the same code path a real fix to the credential would exercise. Corrected the test to assert the accurate real state instead of forcing the code to match a wrong assumption. digest.ts's underlying send-decision logic itself untouched, per the story's explicit out-of-scope. |
| S-95 | Used it up, Used some, Wasted, and the three fraction buttons raised from 32px to a 44px min-height touch target | app/(app)/inventory/[id]/consume-actions.tsx (className="min-h-11" added to all six buttons, size="sm" kept) | e2e consume-touch-targets.spec.ts 1/1, plus consume-fraction-confirm.spec.ts 2/2 and confirmations.spec.ts 3/3 re-run together for regression -- 6/6 clean | 71dea0a | done | Mechanical, same pattern as S-91 -- ConfirmPanel's own Confirm/Cancel buttons (rendered from the separate confirm-panel.tsx, also under 44px at their default md size) deliberately left untouched: out of this story's explicit file scope (consume-actions.tsx only) and shared by every other ConfirmPanel call site (S-69, S-93, S-94), so changing it here would silently resize controls this story never audited. Flagging as a candidate for a future dedicated story if the audit wants it, not fixing opportunistically. |
| S-94 | 25/50/75% fraction consume buttons now require the same ConfirmPanel step as Used it up/Wasted; a fraction/amount tap that resolves to zero surfaces an explicit message instead of a silent {ok:true} no-op | lib/inventory/consume.ts (recordConsumption returns `ConsumeResult = {wrote:true}\|{wrote:false, reason:'nothing_to_log'}` instead of void), app/(app)/inventory/[id]/actions.ts (runAndRevalidate checks result.wrote, returns ok:false with an explicit message when nothing was written), app/(app)/inventory/[id]/consume-actions.tsx (PendingConfirm extended to a `{kind:'fraction', fraction}` variant; the three fraction buttons now open the same ConfirmPanel used-it-up/wasted already use, labeled per-fraction), e2e/consume-fraction-confirm.spec.ts (new, 2 tests) | e2e consume-fraction-confirm.spec.ts 2/2 (independently re-run twice consecutively, clean both times) + e2e confirmations.spec.ts 3/3 (existing suite, re-run to confirm no regression from touching the same ConfirmPanel/PendingConfirm structure) | 0c4fe04 | done | Manual-amount 'Used some' path deliberately left untouched per the story's own invariant (already has its own two-step friction: type an amount, separate Log click) -- confirmed by inspection, not just by omission. One test-authoring bug caught in RED, not an app bug: the cancel-then-confirm test flow redundantly re-clicked "Used some" after Cancel, which toggles `showUsedSome` (state independent of `pendingConfirm`, unaffected by Cancel) back *closed* instead of leaving the already-open fraction row alone -- fixed by removing the redundant click. "Nothing to log" reuses the existing ok:false/error-Alert path rather than adding a third result shape to ActionResult -- pragmatic given every other soft-rejection in this file already renders through that same path, at the cost of the message appearing with the same visual weight as a real error. |
| S-93 | catalog merge now shows a success confirmation naming both items instead of the row silently disappearing | app/(app)/catalog/catalog-manager.tsx (MergePanel's onDone now carries a message string built from itemLabel(loser)/itemLabel(survivor); CatalogManager adds mergeResult state, clears it when a new merge starts, renders it as an Alert tone="success" once merging closes), e2e/catalog-merge-confirmation.spec.ts (new) | e2e catalog-merge-confirmation.spec.ts 1/1 (independently re-run twice consecutively, clean and fast both times once the route was warm) | 4e378c9 | done | Two test-authoring mistakes caught and fixed during RED, not app bugs: (1) two "Merge selected" buttons render (desktop `hidden md:inline-flex` + a mobile fixed-bottom-bar one) -- `.first()` grabbed the hidden desktop button on this repo's default mobile viewport; fixed to `.last()`, matching confirmations.spec.ts's already-established convention for the same dual-button pattern. (2) `CardTitle` renders a plain `<div>`, not a heading element, so `getByRole('heading', ...)` never matches it -- fixed to `getByText(...)`. Also hit the same "action still pending after the default 5s expect timeout" symptom seen elsewhere this session under this sandbox's current load (Confirm merge/Cancel both showed `[disabled]` in the failure snapshot, meaning the transition hadn't resolved yet) -- not a functional bug, resolved by widening that one assertion's timeout to 20s; both a fast rerun afterward (~17s and ~14s total) and the disabled-button evidence rule out a hang. Final test-authoring fix: `getByText('S-93 fixture B')` for the "loser row is gone" assertion also matched the new success message's own text ("Merged S-93 fixture B into..."), so it never reached 0 -- scoped to `getByRole('checkbox', { name: 'S-93 fixture B' })` instead, which only exists in the row. |
| S-92 | near-duplicate review warning now inline-summarizes (merchant/date/total) and links to the actual receipt referenced by near_duplicate_of | lib/review/data.ts (new getDuplicateReceiptSummary, household-scoped), app/(app)/review/[id]/page.tsx (fetches it alongside the existing Promise.all when near_duplicate_of is set, passes duplicateReceipt prop), app/(app)/review/[id]/review-detail.tsx (Alert's action slot gets a "View possible duplicate" Link, body inline-summarizes merchant/date/total when available), e2e/review-duplicate-warning.spec.ts (new) | e2e review-duplicate-warning.spec.ts 1/1 (independently re-run twice consecutively, clean both times) | bccf057 | done | The link goes through confirmDiscardIfDirty (S-90's guard), matching every other navigation-away control on this page -- without it, this would have silently reintroduced the exact discard-unsaved-edits bug S-90 fixed two stories ago, just via a new link. Verified non-blocking invariant explicitly: seeded a matched line so Commit to inventory is enabled, asserted it stays enabled with the warning visible. First run hit `net::ERR_ABORTED` on page.goto under severe transient slowness (dev-server log showed `GET / in 12.7s`, `POST /onboarding in 14.2s` in that same run) -- the server logged a real 200 in 7.4s for the review page itself, confirming this was Playwright's own test-level timeout being exhausted by the slow prior steps, not a crash in the new code; re-ran with a longer --timeout and it passed clean twice. Distinct from the review-states.spec.ts finding recorded under S-91 -- that one is a deterministic 404 unrelated to load; this one was purely a timeout budget issue and resolved on retry. |
| S-90 | unsaved edits on a review line now prompt before "Skip to next" or the mobile back link discards them | components/ui/mobile-top-bar.tsx (optional `onBackClick` prop on the back Link), app/(app)/review/[id]/review-detail.tsx (`reportDirty` callback threaded through NeedsReviewRow/MatchedLineRow/NewItemRow, each row's effect diffs current field state against the original `line` prop and reports dirty/clean, cleared on unmount; top-level `dirtyLineIdsRef` + `confirmDiscardIfDirty` using `window.confirm`; `SessionCounter` gained `onNavigateAttempt`; MobileTopBar moved from page.tsx into review-detail.tsx so its back link can share the same ref), app/(app)/review/[id]/page.tsx (MobileTopBar render removed, now owned by review-detail.tsx), e2e/review-unsaved-edits.spec.ts (new, 2 tests) | e2e review-unsaved-edits.spec.ts 2/2 (independently re-run twice consecutively, clean both times) | a6f2aa3 | done | `window.confirm` deliberately chosen over S-69's `ConfirmPanel` -- ConfirmPanel is for destructive ledger-writing actions (consume, wipe, merge); this is a native "leave without saving?" nav guard where nothing is written either way, and window.confirm is the familiar, lower-code pattern for exactly this case. Hit real e2e instability before landing: the first test's sequential dismiss-then-accept dialog interactions raced against per-click `page.once('dialog', ...)` registration with an unawaited `dialog.dismiss()`, producing shifting failures across runs (element-not-found, click-timeout-despite-visible-element, webServer-start-timeout, full command hang) that looked at first like environment/resource instability rather than a test bug. Diagnosed and fixed by switching to a single persistent `page.on('dialog', ...)` listener per test (registered once, not re-armed per click) driven by a `pendingAction` variable, with the handler awaiting `dialog.accept()`/`dismiss()` internally instead of a fire-and-forget `void` call; 2/2 clean on two consecutive full re-runs after the fix, no further instability. Separately, self-inflicted and recovered: a `git checkout --` intended to revert an errant unscoped `npx prettier` reformat (this repo has no prettier config; running it introduced double-quote style across the touched files) instead reverted the actual S-90 feature code to HEAD, since checkout doesn't distinguish "revert this specific change" from "discard everything uncommitted." Recovered byte-for-byte via `git fsck --dangling` -- the pre-checkout diffs I'd already read matched the recovered blob hashes exactly -- and re-verified with a fresh e2e run before committing. No data loss, but confirms `git checkout -- <file>` should not be used to selectively undo a tool-introduced formatting pass on a file with real uncommitted work; a targeted re-edit or `git diff` review is safer than blanket revert-and-reapply. |
