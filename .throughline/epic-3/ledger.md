| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-11 | is_non_inventory exclusion gate (review_state='excluded'), uncategorized flag, review_state vocabulary definition | lib/receipts/canonicalize.ts, docs/architecture/03-data-model.md | `npm run build` -> pass; live-Supabase integration -> non-inventory line lands excluded/null/null; uncategorized line demotes an otherwise-clean match to needs_review | 879cb1e | done | none |
| S-09 | Canonicalization ladder: seed-alias backfill migration + find_best_alias_match RPC (pg_trgm), exact/fuzzy matching against item_aliases only, review_state='new_item' on miss (no eager catalog_items/item_aliases writes — deferred to E-4 per G6 advisor review) | supabase/migrations/20260730070954_canonicalization_seed_aliases_and_match_rpc.sql, lib/receipts/canonicalize.ts, lib/llm/extract.ts | `npm run build` -> pass; migration pushed to live remote (supabase db push), confirmed 0 orphaned catalog_items post-backfill; live-Supabase integration -> brand-new product flagged new_item with no catalog row created, exact-text match against a test-seeded item resolves matched_item_id with confidence 1.0 and no duplicate catalog_items row, ambiguous/unrelated text does not land 'matched', cross-household identical text does not match | 879cb1e | done | G6 advisor review caught two issues before code was written: (1) seeded catalog had zero aliases, would have silently duplicated all ~300 items on first use — fixed via backfill migration; (2) original spec eagerly created catalog_items/item_aliases at ingest, contradicting F4/F7 ("commit writes aliases") — revised to defer to E-4, added 'new_item' review_state instead |
| S-10 | Unit normalization (kg/l/dozen/pack-of-N/NxM per F5), only runs for S-09-matched lines (target base_unit from the matched catalog item, not a category default); ambiguous unit demotes 'matched' -> 'needs_review', qty_base=1 default; unmatched lines get qty_base=null | lib/receipts/canonicalize.ts | 16 pure-function cases (tsx script, no runner in repo) -> pass; live-Supabase integration -> l->ml x1000 on a matched line, unrecognized unit demotes review_state and defaults qty_base=1 while keeping matched_item_id, new_item line stays qty_base=null | 879cb1e | done | none |
