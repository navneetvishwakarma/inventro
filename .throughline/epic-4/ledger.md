| story | scope | files | tests | commit | status | risks |
|-------|-------|-------|-------|--------|--------|-------|
| S-12 | Review queue split view (`/review`, `/review/[id]`): document preview, mandatory purchase-date confirmation (`purchased_at_confirmed` column), stock_epoch past-order banner + override checkbox, near-duplicate soft warning, per-line resolution (save & match / confirm as new item / mark non-inventory) for needs_review/new_item rows reusing S-09's matching ladder (now exported) | supabase/migrations/20260730071000_review_purchase_date_confirmation.sql, lib/llm/extract.ts, lib/receipts/canonicalize.ts, lib/review/data.ts, app/review/page.tsx, app/review/[id]/page.tsx, app/review/[id]/review-detail.tsx, app/review/[id]/actions.ts, app/page.tsx | `npm run build` -> pass; live-Supabase integration (shared script with S-13, see below) -> confirmPurchaseDate validation + atomic write, saveAndMatchLine resolves exact-text to existing item with correct qty_base, confirmAsNewItem rejects 'uncategorized' and accepts a real category, markLineNonInventory excludes a line | 8c92035 | done | shipped in the same commit as S-13 -- see risk note there |
| S-13 | Commit -> atomic `commit_receipt()` Postgres function: creates catalog_items/item_aliases for new_item lines (E-3's deferred write, now landed), writes stock_movements (+ 'initial' movement on past-order override, mirroring onboarding's pattern), price_history, minimal item_stats upsert (purchase_count/last_purchased_at only -- full prediction engine is E-5). Never re-matches inside the commit loop | supabase/migrations/20260730072000_commit_receipt_function.sql, 20260730073000_commit_receipt_kolkata_day_fix.sql, 20260730074000_commit_receipt_instant_compare_fix.sql, lib/review/commit.ts, lib/date.ts | `npm run build` -> pass; live-Supabase integration, 26/26 + 5/5 boundary assertions -> both commit guards (unconfirmed date, needs_review line remaining) reject with zero writes; A18 backdated without override leaves v_current_stock unchanged for both a matched and a newly-created item; A18 with override increases current stock via the 'initial' movement while the original 'purchase' movement stays at the historical date; two similar-text new_item lines on one receipt create two distinct catalog_items (advisor-caught bug, verified fixed); item_stats.purchase_count/last_purchased_at correctly bookkept across two separate commits of the same item; boundary case (purchase confirmed for the same Kolkata calendar day stock_epoch was set on, but chronologically before its exact instant) correctly excluded without override / included with it | 922c7ec (fix on top of 8c92035) | done | S-12 and S-13 were specced, implemented, and verified as one unit and shipped in a single commit rather than two -- they are not independently buildable (S-12's Commit button has no write path without S-13's RPC) or independently testable (S-13's RPC has no caller without S-12 having produced a committable receipt). Noted as a deliberate deviation from the usual one-commit-per-story pattern, not an oversight. Advisor review before implementation caught a real correctness bug in the original (non-atomic, sequential-write, re-match-on-commit) plan -- fixed via a single atomic Postgres function that trusts each line's pre-resolved review_state and computes new_item qty_base in TypeScript (reusing S-10's normalizeUnitToBase). A second advisor review, done before merge as the "believe the task is complete" check, caught a live bug: `confirmPurchaseDate` built the stored instant via UTC midnight instead of Kolkata midnight (household is fixed to Asia/Kolkata per working spec Sec2), which could misclassify a same-day receipt across the stock_epoch boundary. The first fix attempt (comparing Kolkata calendar days instead of raw instants) was itself wrong -- this epic's own follow-up boundary test caught that v_current_stock's real filter (ADR-0001) is a plain instant comparison, so a calendar-day comparison could disagree with it and silently hide the override option on the day stock_epoch was set. Landed fix: construct the instant correctly (Kolkata midnight) and keep comparisons instant-based throughout -- two small forward-fix migrations (073000, 074000) rather than editing the already-pushed 072000, consistent with additive-only migration discipline |
