Calm "nothing here yet" state — no priced purchases, no waste logged, empty review queue.

```jsx
<EmptyState title="Nothing due yet" description="You're all set." />
<EmptyState title="No items match these filters" action={<Button variant="ghost" size="sm">Clear filters</Button>} />
```
