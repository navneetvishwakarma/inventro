Loading placeholder — shimmer block for item cards / rows while inventory, plan, or insights data loads.

```jsx
<Skeleton height={18} width="60%" />
<Skeleton height={64} radius="var(--radius-md)" />
```
