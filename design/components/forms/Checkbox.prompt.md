Tick-off control for onboarding staples, multi-select in the catalog manager, and the "past order — still on hand" override.

```jsx
<Checkbox id="staple-rice" label="Basmati rice" checked={ticked} onChange={setTicked} />
```
