Standalone field label — `Input`/`Select`/`Textarea` already render their own, so use this only for grouping raw inputs (e.g. a fieldset of checkboxes).

```jsx
<Label htmlFor="budget">Monthly grocery budget</Label>
```
