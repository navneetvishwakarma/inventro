Single-choice control — used for "which item survives" in the catalog merge panel.

```jsx
<Radio name="survivor" id="a" label="Basmati Rice (India Gate) survives" checked={survivor==='a'} onChange={() => setSurvivor('a')} />
```
