Native `<select>` restyled — used for category pickers (recategorize, review-line category) and filter bars (cadence, stock state).

```jsx
<Select label="Category" placeholder="Uncategorized" options={[{value:'dairy',label:'Dairy'},{value:'grains',label:'Grains'}]} />
```
