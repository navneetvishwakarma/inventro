Mobile equivalent of Sidebar — 5 destinations with capture ("Add") raised as a filled center action, matching F2's status as "the highest-traffic surface."

```jsx
<TabBar active="today" onNavigate={setScreen} />
```
