Pill tab group — formalizes the cadence-bucket switcher on Plan (`daily (3)`, `weekly (7)`…).

```jsx
<Tabs items={[{value:'daily',label:'Daily',count:3},{value:'weekly',label:'Weekly',count:7}]} active="weekly" onChange={setBucket} />
```
