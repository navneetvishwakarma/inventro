# Manual test log

> Per-epic manual/E2E checks that aren't automated, appended by `define-epic`
> as each epic's issues are created. See `.claude/epic-<N>/test-plan.md` for
> the full unit/integration/manual breakdown behind each entry here.

## E-0: Foundation & platform scaffolding — [#5](https://github.com/navneetvishwakarma/inventro/issues/5)

- **S-01** [#6](https://github.com/navneetvishwakarma/inventro/issues/6) — deployed Vercel URL loads without error (smoke test).
- **S-02** [#7](https://github.com/navneetvishwakarma/inventro/issues/7) — spot-check ~15 seeded catalog items across categories against working spec §6's stated priors (milk ~2d, rice ~45d, detergent ~60d) to catch a systematically wrong seed before it biases every cold-start prediction.
- **S-03** [#8](https://github.com/navneetvishwakarma/inventro/issues/8) — grep the production build output for the Supabase anon key string (A26, shared with PRD REQ-26's acceptance).
- **Cross-story** — once all three land, confirm E-0's composite acceptance (schema + RLS-disabled + seed data + passcode gate) holds simultaneously on the same live deployment.

## E-3: Canonicalization & matching — [#20](https://github.com/navneetvishwakarma/inventro/issues/20)

- **S-11** [#21](https://github.com/navneetvishwakarma/inventro/issues/21) — feed a 20-line PDF order confirmation fixture (with a delivery fee and GST row) through extraction + the E-3 pipeline; confirm both non-inventory rows land `review_state='excluded'` and every other row is processed normally (A3).
- **S-09** [#22](https://github.com/navneetvishwakarma/inventro/issues/22) — after the seed-alias backfill migration, confirm every one of the ~300 S-02 catalog items now has an alias (no orphans). Then: a brand-new product lands `review_state='new_item'` with no catalog_items/item_aliases row created; a line matching a test-seeded existing item's alias (standing in for a prior commit, since E-4 doesn't exist yet) resolves `matched_item_id` to that same item without creating a duplicate.
- **S-10** [#23](https://github.com/navneetvishwakarma/inventro/issues/23) — a fixture receipt with mixed units (kg, l, dozen, pack-of-N, `2 x 500ml`), each line pre-matched to an existing item, commits to the correct base-unit quantities; a matched line with a nonsense unit string lands `qty_base=1` and `review_state` demoted to `'needs_review'`; a `'new_item'` line stays `qty_base=null`.
- **Cross-story** — once all three land, run one real multi-line receipt (mixed units, one new product, one repeat of a test-seeded item, one fee row) through capture -> extraction -> E-3 end to end on the live deployment; confirm the new product is flagged `new_item` (not auto-created), the repeat resolves without a duplicate, and the fee row stayed excluded.

## E-4: Review & commit — [#25](https://github.com/navneetvishwakarma/inventro/issues/25)

- **S-12** [#26](https://github.com/navneetvishwakarma/inventro/issues/26) — open `/review/[id]` for a receipt with no extractable date: confirm the date field renders unconfirmed and Commit stays disabled; confirm a date manually and confirm Commit becomes available (A21). Resolve one `needs_review` line via "Save & match" and one `new_item` line via "Confirm as new item"; confirm a `category_slug='uncategorized'` new-item confirmation is rejected. Confirm the past-order banner appears/disappears correctly as the confirmed date crosses `household.stock_epoch`.
- **S-13** [#27](https://github.com/navneetvishwakarma/inventro/issues/27) — commit the reviewed receipt from S-12's manual check; confirm `receipts.status='committed'`, the new item now has a real `catalog_items`/`item_aliases` row, and every committed line's item shows correct `qty_base` via `v_current_stock` (A1, Day 1 gate). Repeat with a backdated receipt (before `stock_epoch`): confirm current stock does NOT change without the override checkbox, and DOES change with it (A18) — and that the original purchase movement stays at the historical date either way.
- **Cross-story** — once both land, run one real receipt end to end: capture -> extraction -> E-3 -> S-12 review (resolving every line, confirming the date) -> S-13 commit, entirely on the live deployment. This is the epic's acceptance clause verbatim ("Upload a real receipt, review it, see items in inventory").

## E-5: Prediction engine — [#29](https://github.com/navneetvishwakarma/inventro/issues/29)

- **S-14a** [#30](https://github.com/navneetvishwakarma/inventro/issues/30) — feed computeItemStats a clean 4-purchase weekly sequence and confirm intervals/EWMA/shrinkage by hand-computed arithmetic; confirm a same-Kolkata-day duplicate purchase collapses into one event before interval math runs, not a spurious ~0-day interval.
- **S-14b** [#31](https://github.com/navneetvishwakarma/inventro/issues/31) — a fixture with reliable quantities on >=70% of events produces a rate-blended next-purchase date; a fixture below that threshold falls through to the pure interval-based date with no leaked rate estimate; a short-perishability item's depletion date is capped even when the rate-based estimate would run longer.
- **S-14c** [#32](https://github.com/navneetvishwakarma/inventro/issues/32) — the three headline fixtures: day 0/7/14/21 buckets weekly with confidence ~0.50 and next-purchase ~day 28 (A5, corrected from the working spec's "confidence High" — see epic #29 deviations); day 0/7/60/67 rejects the 60-day outlier and stays weekly (A6); a 2-purchase item is prior-dominated with confidence ~0.25 and bucket=unpredictable (A7). Six consecutive live recomputes of one item never leave more than 5 rows in item_stats_history.
- **S-15** [#33](https://github.com/navneetvishwakarma/inventro/issues/33) — commit a purchase for an item whose previous predicted_depletion_at was already well in the past relative to the new purchase date; confirm rate_correction increases (clamped at 2.0 under repeated fires). Commit a backdated purchase (earlier than an existing later purchase on record) for the same item; confirm rate_correction is untouched.
- **S-16** [#34](https://github.com/navneetvishwakarma/inventro/issues/34) — commit a real multi-line receipt and confirm item_stats/item_stats_history exist for every committed item immediately (synchronous, no queue). Invoke `/api/cron/recompute-stats` directly with a valid CRON_SECRET header against a household with a mix of items (including one with zero purchases); confirm every item with history gets recomputed, the zero-purchase item is skipped without error, and a request with a missing/wrong secret gets 401.
- **Cross-story** — once all five land, commit one real receipt for an item with existing history and confirm in one pass: item_stats reflects the fresh EWMA/confidence/bucket, rate_correction reflects any reconciliation that fired, item_stats_history gained a row (still capped at 5), and a manual nightly-route invocation afterward refreshes the item's other stats without touching rate_correction.

## E-6: Validation harness — [#36](https://github.com/navneetvishwakarma/inventro/issues/36)

- **S-17** [#37](https://github.com/navneetvishwakarma/inventro/issues/37) — with `ENABLE_SEED` unset, confirm `npm run seed:history` refuses to run and writes nothing; with it set, confirm the demo household (`is_demo=true`, fixed id, never `DEFAULT_HOUSEHOLD_ID`) ends up with ~70 catalog_items across the 7 cohorts and a plausible stock_movements count; re-run and confirm it's idempotent (no duplicates); run `npm run seed:history -- --wipe` and confirm the demo household's rows all drop to zero while the real household's row counts are untouched.
- **S-18** [#38](https://github.com/navneetvishwakarma/inventro/issues/38) — after a fresh seed, run `npm run validate:predictions` and confirm it prints the full per-cohort scorecard plus an overall S3 (n>=4) line that clears >=70%; hand-verify one clean_periodic item's held-out prediction error against the script's own printed number; run against a wiped demo household and confirm it fails with a clear "run seed:history first" message rather than crashing.
- **Cross-story** — once both land, `npm run seed:history` then `npm run validate:predictions` in one session reproduces the full Sec12 scorecard end to end on the live deployment's linked Supabase project — this epic's acceptance clause verbatim ("S3 scorecard (n>=4) clears >=70% overall").

## E-7: Inventory & consumption — [#40](https://github.com/navneetvishwakarma/inventro/issues/40)

- **S-19** [#41](https://github.com/navneetvishwakarma/inventro/issues/41) — open `/inventory` on the live deployment: confirm "Out of stock" / "Running low" sections are pinned above the grouped-by-category list; exercise each filter (category, cadence, stock-state, staples-only) and the search box against real data; open one item's `/inventory/[id]` and confirm the plain-language explanation string reflects that item's real cadence/last-purchased/days-remaining numbers, not placeholder text. Confirm no writes occur anywhere in the flow (before/after row count on `stock_movements`/`item_stats`).
- **S-20** [#42](https://github.com/navneetvishwakarma/inventro/issues/42) — on one real item with positive stock, tap "Used it up": confirm stock reads 0 immediately on `/inventory/[id]`, a `type='consumption'` movement was written, and the item now appears in the "Needs attention" section on `/` (A8). Tap "Used it up" again and confirm no duplicate movement is written. On a different item, use "Used some" at 50% and confirm the write is clamped to the pre-action virtual (not raw) stock. Confirm `reconcileRateCorrection`'s consumption-signal gate now finds a real movement for the item that received an action.
- **Cross-story** — once both land, "Used it up" on `/inventory/[id]` is reflected on `/inventory` (list) and `/` (Today stub) in the same session without a manual refresh causing anything unexpected — this epic's acceptance clause verbatim ("Marking an item used updates stock and Today").

## E-8: Cadence planning — [#44](https://github.com/navneetvishwakarma/inventro/issues/44)

- **S-21** [#45](https://github.com/navneetvishwakarma/inventro/issues/45) — open `/plan` on the live deployment: confirm one tab per active cadence bucket, suggested quantities look sane against real `daily_rate_base`/`interval_est_days` for a spot-checked item. Move one real item to a different bucket via the override control; confirm it appears under the new tab immediately and `item_stats.cadence_override` is set. Trigger a real nightly recompute (`/api/cron/recompute-stats`) afterward and confirm the override is untouched (A9). Snooze one item, skip another, exclude a third; confirm each disappears from its bucket with an undo control visible, and reappears correctly after snooze/skip auto-expiry or an explicit undo/revert action.
- **S-22** [#46](https://github.com/navneetvishwakarma/inventro/issues/46) — open `/` and confirm a "Due soon" section lists items with `dueDate` within 3 days (including overdue), distinct from "Needs attention"; confirm an excluded or currently-snoozed item never appears there even if due soon. Confirm a cold-start household (no purchases) renders the existing onboarding nudge without a broken or blank section.
- **Cross-story** — once both land, setting a cadence override on `/plan` is immediately reflected in `/`'s Due soon section and vice versa in the same session — this epic's acceptance clause verbatim ("Manual override survives nightly recompute (A9)").

## E-9: Shopping list — [#48](https://github.com/navneetvishwakarma/inventro/issues/48)

- **S-23** [#49](https://github.com/navneetvishwakarma/inventro/issues/49) — open `/shopping-list` on the live deployment: generate a list from one cadence bucket, confirm the item set matches that bucket's pending items on `/plan` and each row's suggested quantity looks sane; generate again from "due in N days" and confirm the previous list is replaced (archived), not appended to. Confirm an item you've snoozed/excluded on `/plan` never appears on a freshly generated list. Tap "Copy as text" and confirm the copied string matches the on-screen list (checked/unchecked markers, quantities).
- **S-24** [#50](https://github.com/navneetvishwakarma/inventro/issues/50) — on a real shopping-list item, enter a price and log the purchase: confirm it's reflected in inventory (stock increased), the item's prediction moved (`predicted_next_purchase_at`/cadence on `/plan` and `/inventory/[id]`), and the row shows "Logged: ₹X". Confirm double-tapping "Log purchase" on the same row never double-writes. Confirm plain checkbox toggling (no price) never touches inventory or price history. Confirm a real receipt commit afterward for the same item still reconciles correctly (rate_correction responds as expected, not skipped or corrupted by the shopping-list purchase).
- **Cross-story** — once both land, generate a **due-in-N-days** list on `/shopping-list`, log a real purchase for one item with a price, and confirm the item drops off the next regenerated due-in-N-days list (its `predicted_next_purchase_at` moved past the window) — this epic's acceptance clause verbatim ("List generates from a bucket and exports as plain text"). Note: a **bucket-sourced** list is deliberately NOT due-date filtered (S-21's own precedent: a bucket tab lists every item assigned to that bucket, browsable, not just items due today) — a logged purchase does not remove an item from its bucket, only from a due-in-N-days list. Verified live: `predicted_next_purchase_at` moved forward after logging, the item dropped off a regenerated due-in-30-days list, and the same item remained present on a regenerated bucket list for its own cadence bucket.

## E-10: Capture polish & backdating — [#52](https://github.com/navneetvishwakarma/inventro/issues/52)

- **S-25** [#53](https://github.com/navneetvishwakarma/inventro/issues/53) — with the grouping toggle on, select 2-3 real screenshots of one order; confirm exactly one receipt/one ingest job is created with ordered `storage_paths`, extraction runs once (`parse_path='multimodal'`), and the resulting line set is not inflated ~Nx versus a single-image baseline where available. Confirm the toggle rejects a non-2-3 or non-image selection client-side without ever calling the server action. Confirm a mid-upload failure leaves zero orphaned Storage objects.
- **S-26** [#54](https://github.com/navneetvishwakarma/inventro/issues/54) — from a page other than `/add`, paste a real image (Cmd/Ctrl+V); confirm navigation to `/add` and the image uploads via the same pipeline as file-picker/drag-drop. Confirm pasting inside a focused input/textarea (including S-28's own textarea) is never intercepted, and pasting plain text anywhere never triggers navigation or an upload attempt.
- **S-27** [#55](https://github.com/navneetvishwakarma/inventro/issues/55) — upload 3+ files in one batch; confirm the "Review N receipts" link carries a working `session` counter that advances ("1 of 3" -> "2 of 3" -> "3 of 3") after each commit and returns to the queue after the last. Confirm an out-of-band-committed receipt mid-session still renders the counter/Next control rather than a dead-end card. Live-verify A18 on a real backdated commit: `v_current_stock` unchanged without the override checkbox, changed with it; `item_stats` (purchase_count, interval computation) reflects the backdated purchase either way; `rate_correction` unchanged (S-15 reconciliation correctly skipped).
- **S-28** [#56](https://github.com/navneetvishwakarma/inventro/issues/56) — paste a real order-confirmation email body into the new textarea; confirm a `text/plain` receipt is created, extraction completes with `parse_path='text'` (never multimodal), and the review page renders the original pasted text in a `<pre>` block. Confirm pasting the identical text twice is caught as a duplicate.
- **Cross-story** — once all four land, run one real historical-catch-up session end to end: select several old order screenshots/PDFs via the multi-file queue (mixing in one grouped 2-3-image order via the S-25 toggle), walk the full sequential-review counter through each, backdating every purchase date, and confirm at the end that current stock reflects only what's genuinely on hand while every item's frequency/interval stats absorbed the historical purchases — this epic's acceptance clause verbatim ("Three grouped screenshots produce one receipt, no triple-counting (A24)").

## E-11: Manual entry & catalog management — [#58](https://github.com/navneetvishwakarma/inventro/issues/58)

- **S-29** [#59](https://github.com/navneetvishwakarma/inventro/issues/59) — open `/add/manual`: search with an empty query and confirm results are recency-ranked (most recently purchased first); search a misspelled/reworded variant of a real item's name and confirm it still ranks first via trigram similarity. Select a result and confirm quantity is prefilled (not blank) and "Log purchase" writes a real stock_movements row visible on `/inventory/[id]` in two taps. Type a name that clearly matches an existing item but choose "Add as new item" anyway; confirm it's blocked with the existing item surfaced instead of a duplicate catalog_items row. Create a genuinely new item end to end (category picker, quantity+unit, optional price) and confirm it appears in `/inventory` afterward with the correct base_unit for its chosen category.
- **S-30** [#60](https://github.com/navneetvishwakarma/inventro/issues/60) — open `/catalog`: confirm both active and archived items are visible (toggle to show archived). Archive a real item and confirm it drops off `/inventory` and manual-entry search immediately, then unarchive and confirm it returns. Recategorize an item into a category with a different default base unit and confirm the item's base_unit on `/inventory/[id]` is unchanged. Pick two items that are genuinely the same product under different names (or a deliberately created test pair), merge them via the preview-then-confirm flow, and confirm on `/inventory` afterward: only the survivor remains, its purchase history/price history includes both items' prior movements, and a fresh manual-entry search for either original name resolves to the single surviving item (A12).
- **Cross-story** — once both land, log a manual purchase for an item, then find and merge a duplicate of that same item created earlier via a different raw-text spelling; confirm the manually-logged purchase's stock_movements row survives the merge intact under the surviving catalog_items id — this epic's acceptance clause verbatim ("Merging two catalog items consolidates movements and aliases (A12)").

## E-12: Budget & insights — [#62](https://github.com/navneetvishwakarma/inventro/issues/62)

- **S-31** [#63](https://github.com/navneetvishwakarma/inventro/issues/63) — open `/insights` on the live deployment: confirm "Spend vs. budget" shows the current month's real spend broken down by top-level category, with categories that had zero spend this month still listed at ₹0 (not missing), and a clear "no budget set yet" state (households.monthly_budget has no Settings write path yet). Confirm "Next month's committed spend" shows a real, non-zero projected total for a household with purchase history, clearly labeled as a projection; spot-check one item's contribution by hand from its item_stats (daily_rate_base or interval_est_days) and latest price_history row. Confirm "Top 10 spend items" lists real items ordered by trailing-90-day spend. Confirm "Price-change alerts" only shows items with a genuine >15% swing against a trailing average built from at least 3 prior price observations — an item with only 1-2 historical prices must never appear here even if its one available delta looks large. Confirm "Waste report" shows this month's wasted items with a valued amount when a price is known, and an explicit "value unknown" (not ₹0) when it isn't. Confirm a cold-start household with no purchase/price history renders a clean, non-broken empty/zero state across every section — no crash, no blank render.
- Cross-story note: this epic also fixed a real cross-cutting data gap — `price_history` now carries a comparable, verified per-base-unit price (`price_per_base_unit`/`price_basis`) at all three purchase-write sites (receipt commit, shopping-list checkoff, manual entry). Confirm on a real purchase logged through any of those three flows that the item's price shows up correctly in Insights' totals on the next `/insights` load (not just in the item's own price history) — the whole feature's numbers depend on that write-time correctness, not just Insights' own read-side code.

## E-13: Notifications — [#65](https://github.com/navneetvishwakarma/inventro/issues/65)

- **S-32** [#66](https://github.com/navneetvishwakarma/inventro/issues/66) — invoke `/api/cron/digest-daily` directly with a valid `CRON_SECRET` header: with nothing due within 3 days, confirm the response is an honest `sent: false` with reason `"nothing due within 3 days"` and no attempt is made to call Resend; with something due, confirm the response is `sent: false` with reason `"RESEND_API_KEY not configured"` (its genuine current state in this environment) or `"no notify_email configured"`, never a fabricated `sent: true`. Repeat for `/api/cron/digest-weekly` at the 7-day window. Confirm both routes 401 on a missing/wrong `Authorization` header, same as `/api/cron/recompute-stats`. Open `/` on the live deployment and confirm the "View plan" link shows a `(N)` badge matching the same due-soon count already rendered in the "Due soon" card, and shows no badge when nothing is due.
- **Known gap, not fixed by this epic:** `RESEND_API_KEY` is unconfigured and `households.notify_email` has no Settings write path yet (S-33/E-14) — so no real email has been sent end to end from this environment. The send code path itself (plain `fetch` to Resend's API) is complete and will work once both are configured; this is a residual risk to close out when Settings/E-14 ships a way to set `notify_email` and a real Resend API key is added to the Vercel project.
- **Incidental fix (not E-13 scope):** `lib/receipts/canonicalize.ts`'s `normalizeUnitToBase` no longer undercounts `qty_base` for multi-pack purchases (the `"N x Munit"` branch, e.g. `"2 x 500ml"`) when more than one pack is bought — commit a real receipt with a `pack_size` like `"2 x 500ml"` and `qty_display="2"` for an item whose base unit is `ml`; confirm the resulting `qty_base` is `2000`, not `1000`.

## E-14: Settings & cost controls — [#68](https://github.com/navneetvishwakarma/inventro/issues/68)

- **S-33** [#69](https://github.com/navneetvishwakarma/inventro/issues/69) — open `/settings` on the live deployment: edit household name/monthly budget/notify email and confirm each round-trips after a reload; try a negative budget and a garbage email and confirm both are rejected with a visible error, no write occurs. Confirm the "Manage catalog" link goes to the existing `/catalog` page. Trigger both exports and confirm the downloaded JSON parses and contains only the real household's data (no demo-household rows), and the CSV opens as a sane purchase-ledger table. Trigger "Wipe demo data" and confirm (via a live row-count check) only the demo household's rows changed — the real household's data is untouched — then re-seed the demo household (`npm run seed:history`) to restore the fixture.
- **S-34** [#70](https://github.com/navneetvishwakarma/inventro/issues/70) — on `/settings`, confirm the cost meter shows a real month-to-date spend figure, a per-receipt average, and a count broken down by model tier that matches the real receipts table for the current Kolkata month. Confirm today's ingestion count is shown, with the display switching to a warning state at 50 and a "limit reached" state at 100 (verify the boundary via a controlled fixture insert/cleanup, not by uploading 100 real receipts). Confirm an upload attempted while today's count is already at 100 is rejected before any Storage write or LLM call occurs.
- **Cross-story** — once both land, setting `notify_email` on `/settings` and confirming (via the digest cron routes) that the "no notify_email configured" skip reason no longer fires for that path — this epic's other prerequisite (a real `RESEND_API_KEY`) remains a separate, external, unconfigured gap, called out honestly rather than papered over.

## E-15: PWA & ship polish — [#72](https://github.com/navneetvishwakarma/inventro/issues/72)

- **S-35** [#73](https://github.com/navneetvishwakarma/inventro/issues/73) — on an Android phone with Chrome, open the production URL, confirm the browser offers "Install app" / "Add to Home Screen", install it, and confirm it launches standalone (no browser chrome). From Android's Photos app, share a real grocery photo to the installed Inventro app via the share sheet; confirm it lands on `/review` with a new receipt already ingesting, applying the same duplicate/guard checks as a manual upload. Turn off Wi-Fi/data after having loaded the app once; confirm reopening it shows a minimal offline page rather than a browser-native "no internet" error, and confirm no stale inventory/plan data is ever shown while offline (shell-only caching, no data sync).
- **S-36a** [#74](https://github.com/navneetvishwakarma/inventro/issues/74) — on a real mobile device (or Chrome DevTools device emulation at ~375px width), walk every route (Today, Inventory, Plan, Shopping List, Insights, Catalog, Review, Add, Add manual, Settings, Onboarding) and confirm no horizontal scroll/clipped content and comfortably tappable buttons. Toggle the OS to dark mode and confirm the app switches without a reload and every status color (errors, success confirmations, warning/info banners) stays legible, not muddy. Visit a nonexistent URL and confirm a styled 404, not Next's default. If reachable, force a Server Component error locally and confirm the styled error boundary with a working retry.
- **S-36b** [#75](https://github.com/navneetvishwakarma/inventro/issues/75) — from a machine with no gate cookie, confirm the production URL returns 401. Confirm (via the ledger's A1-A26 table) every acceptance test has a recorded pass and verification method. This is the terminal ship gate for the v1 backlog — once merged, re-confirm no story anywhere in `docs/engineering/backlog.json` is left not-`done`.

## E-16: Mobile navigation & design-system fidelity remediation — [#80](https://github.com/navneetvishwakarma/inventro/issues/80)

- **S-37** [#81](https://github.com/navneetvishwakarma/inventro/issues/81) — on a real mobile device (or ~375px emulation), confirm Settings and Shopping List are each reachable via at least one tap from Today; empty the review queue (or simulate it) and confirm `/review` is still reachable, not just its badge link. Tab-key through a Switch and a Radio control and confirm both show a visible focus ring. Run `npm run dev` and submit the gate passcode over plain `http://localhost` — confirm it authenticates (cookie set, gate passed). Force a catalog merge-preview RPC failure (e.g. a malformed item id) and confirm a visible error `Alert` renders instead of "Confirm merge" staying silently disabled.
- **S-38** [#82](https://github.com/navneetvishwakarma/inventro/issues/82) — at ~375px width, walk every app route and confirm a title bar renders at the top of each; confirm it disappears at >=1024px (desktop Sidebar takes over). On Review Detail and Inventory Detail, confirm the back button returns to the parent list.
- **S-39** [#83](https://github.com/navneetvishwakarma/inventro/issues/83) — at 375px: on Inventory, confirm the 5-control filter row is replaced by a single "Filters (N active)" trigger, and that N updates correctly as filters are applied. On Plan, confirm the bucket tabs scroll horizontally rather than wrapping, and confirm a mobile item row shows only Snooze/Skip plus an overflow trigger (Always exclude / Move to hidden behind it). On Catalog with 2+ items selected, confirm "Merge selected" is pinned to the bottom of the viewport while the list scrolls underneath. On Review Detail, confirm the Commit button stays pinned to the bottom while line items scroll. Re-check all five at >=1024px and confirm no regression from the pre-fix desktop layout.
- **S-40** [#84](https://github.com/navneetvishwakarma/inventro/issues/84) — on Insights, confirm a spend figure over ₹1,000 renders with a thousands separator (e.g. `₹8,420.00`). Cross-check the same formatting on Review queue, Review detail, Inventory list/detail, and Shopping list (including a logged purchase price). On Review Detail, confirm a matched line's quantity shows a human unit (e.g. "1kg"), not a raw base-unit number. On Inventory Detail, enter `0` then `-1` into the consume amount field and confirm the Log button stays disabled or a visible validation error appears — no silent no-op. Confirm a price-trend Sparkline renders in a neutral tone by default, not brand red.
- **S-41** [#85](https://github.com/navneetvishwakarma/inventro/issues/85) — mouse-down-hold a secondary Button and confirm its color changes (active state present); visually compare a destructive Button's rest/hover/active colors against `design/components/forms/Button.jsx`'s rendered output. Resize a page using Table or ListRow from >=768px down to 375px and confirm the body text visibly shrinks (matches Card's existing behavior). Inspect a Table header cell's HTML and confirm `scope="col"`. On Add·Manual's "new item" panel and Review Detail's editable line fields, confirm every Select/Input has a visible, associated label. With OS-level "reduce motion" enabled, confirm the skeleton shimmer no longer animates.
- **Cross-story** — once all five land, do a full mobile click-through (~375px) from a cold `/gate` login through Today, Add, Review, Inventory, Plan, Shopping List, Insights, Catalog, and Settings without ever needing to resize to desktop width or type a URL by hand — this epic's acceptance clause verbatim ("Every screen is reachable from a mobile-width viewport").
